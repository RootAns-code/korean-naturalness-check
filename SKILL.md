---
name: korean-naturalness-check
version: 2.8.2
description: Use this skill whenever the user wants to evaluate the naturalness of a Korean blog post or any Korean text — checking for translation-style awkwardness (번역투), magazine-formal tone (매거진투), AI-typical phrasings (AI투), or stiff bureaucratic prose (직역체). Trigger on phrases like "한국어 평가", "자연스러움 검수", "어색한지 봐줘", "이 글 검수", "번역투 점검", "이 본문 평가해줘", or when the user gives a Korean .md file or pastes Korean text and asks for review/checking. Also trigger right after another blog-writing skill (naver-blog-studio-v2, naver-blog-review-writer, naver-shopping-connect 등) finishes producing a draft and the user asks for evaluation. Use this skill instead of attempting naturalness review yourself — LLMs have a known bias toward rating their own output as natural, and this skill provides external statistical signal that catches what self-review misses.
---

# 한국어 자연스러움 평가 스킬

## 스킬 목적

LLM이 자기가 쓴 한국어 본문을 자기가 검수하면 자연스럽다고 판정하는 편향이 있습니다. 이 편향 때문에 매거진투, 번역투, AI투 같은 어색한 표현이 자체 검수에서 통과돼 사용자에게 그대로 전달되는 사례가 누적됐습니다.

이 스킬은 형태소 분석기(`kiwipiepy`), 어색 어휘 사전, 그리고 한국어 자연 코퍼스(Leipzig kor_news_2022_1M) 기반 명사+동사 결합 빈도 룩업을 결합한 정량 검수 도구를 통해 LLM 자체 검수와 다른 분포의 외부 시그널을 제공합니다. 코퍼스 결합 빈도 룩업은 사전에 등록되지 않은 어색 표현(예: "기업을 묶다", "사례를 묶음으로")도 자동 검출합니다. 이 스킬의 책임은 의심 문장을 검출하고 권장 수정 방향을 제시하는 것까지입니다. 본문 자동 수정은 글 작성 스킬이나 사용자 후속 지시에서 처리합니다.

출력에는 **문단 설계 층위**(`document_patterns`)가 함께 나옵니다. 시그널 1~5는 전부 문장 단위라, 문장을 아무리 따로 봐도 안 보이는 어색함이 있습니다. 어휘 층위 번역투(이중피동, 이중조사, have/make 직역, 한자어 명사화)를 다 잡아도 "앞 문장이 선언만 하고 뒤 문장이 설명하는" 구조는 남습니다. 이 구조가 문장을 끊고, 짧아진 문장에 정보를 넣으려다 수식이 붙습니다.

이 층위는 **코드가 검출하지 않습니다.** 판정 기준이 의미 관계라 표층 특징으로 근사되지 않기 때문입니다. 대신 코드가 인접 문장 쌍이라는 판정 단위를 만들고, 4단계에서 모델이 순회하며 판정합니다. 검출은 코드, 판정은 모델이라는 이 스킬의 기본 구도에서 검출만 빠진 형태이며, 본문을 직접 고치지 않는다는 원칙은 그대로입니다.

<workflow>

## 워크플로우 4단계

1단계 입력 정규화: 사용자 입력을 파일 경로로 통일
2단계 의존성 확인: `kiwipiepy` 설치 여부 확인
3단계 검수 스크립트 실행: `scripts/check_naturalness.py --json`
4단계 사용자 보고: JSON 결과를 가공해 한국어 보고 + `sentence_pairs` 순회 판정

코드가 출력한 JSON에는 모델의 판정과 수정안이 들어 있지 않습니다. 모델이 `review_required`를 수행한 결과는 별도 보고로 작성합니다. 호출 프로젝트는 이 보고를 받아 본문에 반영하며 판정 기준을 따로 복제하지 않습니다.

각 단계는 이전 단계 결과 위에 쌓이므로 순서대로 진행합니다. 4단계는 두 패스입니다. 문장 단위 적발(`suspects` → `review_band`)을 처리한 뒤, 문단 설계(`sentence_pairs` 순회)를 별도로 처리합니다. 앞 패스는 스크립트가 검출한 것을 가공하는 일이고, 뒤 패스는 스크립트가 만든 판정 단위를 모델이 직접 판정하는 일이라 성격이 다릅니다.

</workflow>

## 1단계: 입력 정규화

검수 스크립트는 마크다운 파일 경로를 입력으로 받습니다. 사용자가 어떤 형태로 본문을 제공하든 파일 경로로 통일한 뒤 다음 단계로 갑니다.

<input_handling>

**파일 경로로 받은 경우**: 사용자가 `/mnt/user-data/outputs/foo.md` 같은 경로를 주거나, 직전에 다른 스킬이 만든 산출물의 위치를 가리킬 때 그 경로를 그대로 사용합니다.

**인라인 텍스트로 받은 경우**: 사용자가 채팅창에 본문을 붙여넣고 평가를 요청할 때 텍스트를 `/tmp/knc_input.md`에 저장한 뒤 그 경로를 사용합니다.

```bash
cat > /tmp/knc_input.md << 'EOF'
[사용자가 제공한 본문 전체]
EOF
```

**입력이 모호한 경우**: 사용자가 "이 글 평가해줘"라고만 하고 본문 위치를 명시하지 않으면 한 번 묻고 진행합니다. 추측해서 임의 파일을 검수하지 않습니다. 잘못된 파일을 검수하면 사용자가 결과를 보고도 해당 파일과 매칭하기 어려워 혼란이 커집니다.

</input_handling>

## 2단계: 의존성 확인

`scripts/check_naturalness.py`는 한국어 형태소 분석기 `kiwipiepy`가 필요합니다. 처음 실행 시 설치합니다.

```bash
pip install --break-system-packages kiwipiepy 2>&1 | tail -3
```

이미 설치돼 있으면 즉시 종료합니다. Claude.ai 웹 컨테이너와 PC 환경 양쪽에서 설치 가능 검증됨.

## 3단계: 검수 스크립트 실행

JSON 모드로 실행해 후처리 가능한 형태로 결과를 받습니다. 텍스트 모드 출력은 사람이 바로 읽기 좋게 정리되지만, 4단계에서 LLM이 한국어 감각으로 추가 가공하려면 JSON이 더 적합합니다.

```bash
python3 scripts/check_naturalness.py <파일 경로> --json
```

<execution_environment>

**실행 환경에 맞춰 커맨드를 고칩니다.** 위 줄은 리눅스·웹 컨테이너 기준이며, 그대로 복사하면 환경에 따라 세 군데가 깨집니다. 이 스킬을 글 작성 파이프라인이 호출할 때는 아래를 확인한 뒤 실행합니다.

| 확인할 것 | 대응 |
|---|---|
| `python3` 명령 유무 | 없으면 `python`을 씁니다. Windows에는 보통 `python`만 있습니다 |
| 스크립트 경로 | **절대경로로 씁니다.** 작업 디렉토리는 호출한 프로젝트 폴더라 `scripts/...` 상대경로가 맞지 않습니다 |
| 한글 출력 깨짐 | `PYTHONIOENCODING=utf-8`을 붙이고 **결과를 파일로 받습니다.** 콘솔 직출력은 코드페이지 때문에 깨져서 의심 문장을 읽을 수 없습니다 |

Windows(PowerShell) 예시입니다. `{스킬폴더}`는 이 SKILL.md가 있는 폴더입니다.

```
$env:PYTHONIOENCODING='utf-8'; cmd /c "python `"{스킬폴더}\scripts\check_naturalness.py`" `"{대상파일}`" --json > `"{출력경로}`" 2>&1"
```

**호출자가 출력 경로를 지정했으면 그 경로에 저장합니다.** 파이프라인은 이 파일을 감사 기록으로 남기므로 임의 위치에 쓰면 흐름이 끊깁니다. 지정이 없으면 대상 파일과 같은 폴더에 `knc_result.json`으로 씁니다.

전용 가상환경에만 `kiwipiepy`가 설치된 환경(클라우드 루틴 등)에서는 그 환경의 python 실행 파일을 직접 지정합니다. 실행에 실패하면 **검사를 건너뛰었다는 사실을 그대로 보고합니다.** 자기 점검으로 대체하고 통과한 것처럼 보고하지 않습니다.

</execution_environment>

스크립트가 반환하는 JSON 구조는 다음과 같습니다.

```json
{
  "file": "...",
  "total_sentences": 8,
  "suspect_count": 3,
  "average_score": 22.4,
  "threshold": 17,
  "corpus_signal_active": true,
  "signal_meaning": "corpus_collocation freq=0은 코퍼스 기준 양성이다. '거짓양성'은 모델이 붙이는 미검증 라벨일 뿐이므로, 명사 의미를 확인해 동사 적합성을 판정하기 전엔 일축 금지 (전문은 실제 출력 참조)",
  "oov_nouns": ["닌텐도", "스위치"],
  "suspects": [
    {
      "sentence": "본문 문장",
      "score": 60.0,
      "breakdown": {
        "noun_density": 0,
        "noun_verb_ratio": 20,
        "bound_josa_chains": 0,
        "awkward_lexical": 40,
        "corpus_collocation": 0
      },
      "matched": ["측면에서", "것으로\\s*[보평확나사판알전풀]"],
      "matched_groups": ["보고서투_명사구", "것으로_판단동사"],
      "awkward_collocations": [],
      "oov_nouns": []
    },
    {
      "sentence": "분야별 관심 기업이 다음과 같이 묶입니다.",
      "score": 25.3,
      "breakdown": {
        "noun_density": 5.3,
        "noun_verb_ratio": 8.0,
        "bound_josa_chains": 0,
        "awkward_lexical": 0,
        "corpus_collocation": 12
      },
      "matched": [],
      "matched_groups": [],
      "awkward_collocations": [
        {
          "rel": "nv",
          "noun": "기업",
          "verb": "묶이",
          "freq": 0,
          "sig": 0.0,
          "corpus_associations": [
            {"verb": "거듭나", "freq": 49, "sig": 343.9},
            {"verb": "늘", "freq": 84, "sig": 239.0}
          ]
        },
        {
          "rel": "vn",
          "noun": "두껍",
          "verb": "신뢰",
          "freq": 0,
          "sig": 0.0,
          "corpus_associations": [
            {"verb": "변함없", "freq": 9, "sig": 85.9},
            {"verb": "두텁", "freq": 9, "sig": 85.2},
            {"verb": "깊", "freq": 16, "sig": 75.7}
          ]
        }
      ],
      "oov_nouns": []
    }
  ],
  "review_band_count": 2,
  "review_band": [...],
  "review_band_meaning": "12점 이상 임계 미만 문장. 임계 아래는 통과가 아니라 미보고이므로 suspects 처리 후 반드시 훑어 판정하라는 소비자 지시 (전문은 실제 출력 참조)",
  "all_results": [...]
}
```

`awkward_collocations`의 `rel`은 결합 관계입니다 — `nv`: 명사→용언("기업이 묶이다"), `vn`: 형용사 관형형→명사("두꺼운 신뢰"), `av`: '-히'계 부사→용언("급격히 진행하다"). `noun`/`verb` 키는 관계의 왼쪽/오른쪽 요소입니다(하위 호환 명명). 하다류 용언은 nv에 포함됩니다.

`corpus_associations`는 적발된 결합의 축 단어가 코퍼스에서 자주 만난 연관어입니다 — nv면 그 명사의 잦은 용언, vn이면 그 명사의 잦은 수식어, av면 그 용언의 잦은 부사. **문장을 보지 않는 단어 단위 빈도 통계라 문맥 비인지이며, 대체 표현 후보가 아닙니다.** "기업이 묶이다" 적발에 거듭나/늘이 나오는 것처럼 원문 의미와 무관한 단어가 상위에 올 수 있습니다. 이 필드는 "이 단어는 원래 이런 결합으로 쓰인다"는 적발 근거 확인용으로만 참고하고, 사용자 보고에는 기본적으로 노출하지 않습니다.

**단일 수식 적발(12점)은 기본 임계(17)를 넘지 않습니다** — 코퍼스 0건의 모호성(어색 vs 자연이지만 미수록) 판정을 LLM에 위임하는 설계입니다.

**임계는 기본값(17)으로 두고 `review_band`를 전량 판정하십시오.** `--threshold`를 12로 낮추지 마십시오. 낮추면 밴드가 `suspects`로 흡수되는데, `suspects`는 소비 파이프라인이 재작성을 강제하는 구간이라 **모호성 판정을 LLM에 위임한다는 이 설계가 무너지고 12점 적발까지 기계적으로 재작성돼 과교정이 납니다.** 두 구간은 처리 성격이 다르므로 분리된 채로 받아야 합니다.

| 구간 | 필드 | 처리 |
|---|---|---|
| 임계 이상 | `suspects` | 재작성 우선. 원문 유지는 근거를 남길 때만 |
| 12점 ~ 임계 미만 | `review_band` | 전량 판정. 어색하면 고치고 자연스러우면 근거 한 줄 |

`review_band`를 rel이 vn·av인 것으로 좁히지 마십시오. nv(명사→용언) 결합도 이 구간에 들어오며, 좁히면 그만큼 미보고로 샙니다.

이 재판단 밴드는 `review_band` 필드로 **항상 출력에 동봉**되고 처리 지시가 `review_band_meaning`에 실려 나갑니다. 이 SKILL.md를 읽지 않고 스크립트만 호출하는 파이프라인에서도 소비자 모델이 JSON만 읽고 이 구간을 처리하게 만들기 위한 설계입니다. `review_band`가 비어 있는데 `review_band_count`가 0이 아니면 임계를 낮춰 실행한 것이므로, 기본 임계로 다시 돌려 두 구간을 분리해 받으십시오.

### `document_patterns` — 문단 설계 층위

JSON 최상위에 다음 블록이 함께 나옵니다.

```json
"document_patterns": {
  "active": true,
  "paragraph_count": 29,
  "sentence_count": 64,
  "measured": {
    "lead_total": 27, "short_lead_count": 13, "short_lead_ratio": 0.481,
    "pair_count": 35,
    "note": "심각도 참고용 통계이지 대상 목록이 아니다. ..."
  },
  "sentence_pairs": [
    {"paragraph": 4, "position": "1-2",
     "lead": "방향이 완전히 무너진 건 수요일입니다.", "lead_chars": 21,
     "follow": "다우가 2.19% 빠지면서 4대 지수가 전부 내렸고 이날이 주간 최저점이었습니다.",
     "first_person_lead": false}
  ],
  "tail_sentences": [
    {"paragraph": 26, "position": "3/3", "chars": 37,
     "text": "AI 인프라 주문이 예전보다 규모도 크고 기간도 길어졌다는 게 이유입니다.", "solo": false}
  ],
  "review_required": [
    {"id": "pair_relation", "unit": "sentence_pairs",
     "target": "예고 또는 선언 뒤에 설명을 분리한 인접 쌍 (문장 사이의 관계)",
     "test": "삭제 검사 뒤에도 문장 관계를 확인하고 정보 보존 재구성문을 비교하라. ...",
     "prescription": "lead의 정보를 follow 안으로 옮겨 한 문장으로 다시 써라. ..."},
    {"id": "cleft_structure", "unit": "sentence_pairs[].lead + tail_sentences[].text",
     "target": "문장 하나로 판정되는 쪼개진 구문 (문장 내부의 구조)",
     "test": "되돌리기 테스트: \"~한 건 X이다\"를 \"X가 ~했다\"로 바꿔 본다. ...",
     "prescription": "문장을 합치지 말고 구문만 푼다. ..."}
  ],
  "review_note": "두 검사는 독립이며 판정 단위도 다르다. ...",
  "confirm_only": { "clause_link_rate": 0.2, "long_sentence_count": 0, "note": "..." },
  "meaning": "문단 설계 층위다. ..."
}
```

**이 블록에는 임계도 적발 목록도 없습니다.** 코드는 판정 단위를 만드는 데까지만 관여하고, 실제 판정은 4단계에서 모델이 합니다. 역할이 필드로 나뉩니다.

| 필드 | 누가 | 무엇 |
|---|---|---|
| `measured` | 코드 | 심각도 참고용 통계. **대상 목록이 아님** |
| `sentence_pairs` | 코드 | 검사1의 판정 단위. 문단마다 인접 문장 2개씩 창을 민 쌍 |
| `tail_sentences` | 코드 | 검사2의 보완 단위. 각 문단의 마지막 문장 |
| `review_required` | 모델 | 두 검사의 절차·테스트·처방·보고 형식 |
| `confirm_only` | 코드 | 수정 후 확인용. **직접 겨냥 금지** |

### 검사가 둘인 이유

성격이 다릅니다. 하나는 문장 **사이의 관계**를, 하나는 문장 **내부의 구문**을 봅니다.

| | 검사1 `pair_relation` | 검사2 `cleft_structure` |
|---|---|---|
| 본다 | 예고 또는 선언과 설명을 불필요하게 분리했는가 | 이 문장이 쪼개진 구문인가 |
| 단위 | 인접 쌍 | 문장 하나 |
| 테스트 | 삭제 검사 후 정보 보존 재구성문 비교 | `~한 건 X이다`를 `X가 ~했다`로 되돌리기 |
| 처방 | 합치기 | 구문만 풀기(문장은 유지) |

**한 문장이 양쪽에 걸릴 수 있습니다.** 그때는 합치기만 적용하면 구문도 함께 풀립니다. 반대로 검사1에서 유지한 문장이 검사2에서는 대상일 수 있습니다 — "목요일부터 시장을 되돌린 건 결국 실적이었습니다"는 지우면 정보가 사라지지만 분열문입니다.

### 판정 테스트

판정 절차의 기준은 JSON의 `document_patterns.review_required`다. 각 항목의 `test`, `prescription`, `reporting`을 끝까지 수행한다. `pair_relation`은 삭제 검사와 재구성 비교를 모두 요구한다. 삭제하면 정보가 사라진다는 결과는 문장 구조를 유지할 근거가 아니다.

예를 들어 “이 요금제를 유지하고 있습니다. 작업을 잘게 나누면 한도가 부족하지 않기 때문입니다.”는 어느 문장을 지워도 정보가 사라진다. 그래도 “작업을 잘게 나누면 한도가 부족하지 않아서 이 요금제를 유지하고 있습니다.”처럼 원인과 결론을 연결한 대안을 작성해 비교한다.

“세션이 초기화돼도 주간 사용량은 계속 누적됩니다. 주간 한도는 계정에 정해진 요일과 시간에 초기화됩니다.”도 “주간 사용량은 세션 한도가 초기화된 뒤에도 계속 누적되다가 계정에 정해진 요일과 시간에 초기화됩니다.”로 정보를 보존할 수 있다.

재구성하면서 인과관계를 새로 만들거나 사실, 조건, 확실성, 화자를 바꾸지 않는다. “요금은 100달러입니다. 모바일 결제 금액은 다를 수 있습니다.”처럼 독립된 안내는 억지로 인과문으로 바꾸지 않는다. 합치면 과도하게 길어지거나 문맥상 결론을 먼저 강조할 필요가 있으면 비교 근거를 남기고 원문을 유지한다. 문단 경계에 걸친 쌍은 추가하지 않는다.

`measured.short_lead_ratio`는 대상 선별에 쓰지 않습니다. 길이는 원인이 아니라 상관 지표이며, 이 값은 "이 글이 얼마나 심한가"의 참고선입니다. 기준선은 문제로 지적된 글 0.48, 사람이 직접 손본 글 0.16입니다.

`sentence_pairs`가 비어 있으면 2문장 이상 문단이 없다는 뜻입니다. 한 줄에 한 생각을 쓰는 SNS 글이 여기 해당하며, 관계 검사가 자연히 침묵합니다. 구문 검사는 `tail_sentences`로 계속 작동합니다.

`long_sentence_count`가 `null`이면 종결부호 밀도가 낮아(SNS 톤처럼 마침표를 생략하는 글) 문장 분리가 성립하지 않으므로 측정하지 않았다는 뜻입니다. 장르를 추정하는 대신 측정 성립 여부만 보므로, 오판해도 침묵할 뿐입니다.

`confirm_only`를 **직접 겨냥하면 안 됩니다.** "절 연결 비율을 올려라"는 지시는 절 경계 쉼표(다수 프로젝트에서 0건 필수)를 부르고, "장문을 만들어라"는 지시는 수식어를 붙여 문장을 늘리게 만들어 정확히 피하려던 증상을 만듭니다.

`oov_nouns`(최상위·문장별)는 코퍼스에 아예 없어 시그널 5가 판정 불가로 침묵한 명사 목록입니다. 코퍼스 기준 시점(2022) 이후 신조어·고유명사가 주로 여기에 잡히며, 이 명사들의 결합은 **검사를 통과한 것이 아니라 검사를 받지 않은 것**입니다.

`corpus_signal_active`가 `false`로 나오면 동봉 코퍼스 DB(`assets/kor_collocation.db`)가 누락된 환경입니다. 이 경우 시그널 5는 비활성이지만 1~4번 시그널은 정상 동작합니다. 사용자 보고 시 코퍼스 룩업 결과 없이 진행하면 됩니다.

## 4단계: 사용자 보고

JSON 결과를 가공해 한국어로 보고합니다. 정규식 패턴은 사용자에게 정보 가치가 없습니다. 사용자는 어떤 문장이 어색하고 어떻게 다듬을지를 알고 싶어합니다. 그래서 LLM이 정규식을 한국어 설명으로 옮기고, 자연스러운 대안을 1~2개 제시합니다.

<output_format>

다음 형식으로 보고합니다.

```
## 한국어 자연스러움 평가

검사 문장 N개 중 의심 문장 M개 (평균 점수 X.X)

### 의심 문장 1 (점수 60.0)
> [원문 문장]
- 어색 신호: [matched 패턴을 한국어로 풀어서 설명]
- 권장 수정: [자연스러운 대안 1~2개]

### 의심 문장 2 (점수 40.0)
> ...

## 문단 설계

**관계** — 인접 쌍 N개 중 M개가 대상입니다.

| 위치 | 앞 문장 | 판정 |
|---|---|---|
| 문단4 1-2 | 방향이 완전히 무너진 건 수요일입니다. | 대상 — 지워도 뒤 문장이 온전 |
| 문단5 1-2 | 목요일에는 나스닥이 2.78% 올랐습니다. | 아님 — 수치가 사라짐 |

> [lead] / [follow] → [합친 문장]

**구문** — 분열문 K개.

| 문장 | 되돌린 문장 |
|---|---|
| 그런데 시장이 본 건 ... 돌아왔느냐였습니다 | 그런데 시장은 ... 돌아왔느냐를 봤습니다 |

수정 후 다시 평가받으시면 됩니다.
```

</output_format>

### 검사 유효성 확인 (보고 전 필수)

적발 건수를 보고하기 전에 **이번 실행이 온전한 검사였는지** 먼저 확인합니다. 반쪽으로 돈 검사의 "적발 0건"을 통과로 보고하면, 검사기를 안 돌린 것보다 나쁩니다. 사용자가 검증됐다고 믿게 되기 때문입니다.

| 필드 | 값 | 보고에 반드시 넣을 것 |
|---|---|---|
| `corpus_signal_active` | `false` | **시그널 5(코퍼스 결합)가 비활성이었다는 사실.** DB 누락 환경이며 1~4번만 돌았습니다 |
| `oov_nouns` | 비어 있지 않음 | 그 명사가 낀 문장은 **검사를 통과한 게 아니라 받지 않은 것**입니다 |
| `document_patterns.active` | `false` | 문단 설계 층위가 측정되지 않았다는 사실 |

세 항목 모두 정상일 때만 "검사 범위 안에서 이상 없음"이라고 쓸 수 있습니다.

### 수정 후 확인 (`confirm_only`)

`review_required`의 처방을 적용한 뒤 검사기를 다시 돌려 `confirm_only`의 값이 어떻게 움직였는지 확인합니다. 이 블록은 **처방 대상이 아니라 처방의 효과를 보는 계기판**입니다.

| 값 | 처방이 들었다면 |
|---|---|
| `clause_link_rate` | 쌍을 합쳤으므로 오릅니다 |
| `long_sentence_count` | 합친 만큼 늘 수 있습니다. 급증했다면 과교정입니다 |
| `document_patterns.measured.short_lead_ratio` | 예고 문장을 흡수했으므로 내려갑니다 |

**이 값들을 직접 겨냥해 문장을 고치면 안 됩니다.** "절 연결 비율을 올려라"는 지시는 절 경계 쉼표를 부르고, "장문을 만들어라"는 지시는 수식어를 붙여 정확히 피하려던 증상을 만듭니다. 처방을 적용한 결과로 움직였는지만 봅니다. 움직이지 않았다면 처방을 실제로 반영하지 않은 것이므로 되돌아가 확인합니다.

### 문단 설계 보고 원칙

`sentence_pairs`와 `tail_sentences`가 모두 비어 있으면 이 섹션 자체를 만들지 않습니다. "문단 설계는 양호합니다" 같은 문장도 쓰지 않습니다 — 검사하지 않은 것을 통과로 보고하는 셈이 됩니다. 쌍만 비어 있으면(SNS 등) 구문 검사 결과만 보고합니다.

**두 검사를 다 돌립니다.** 관계 검사만 하고 끝내면 문단 마지막 문장이 통째로 빠집니다.

**단위 전체를 순회합니다.** 눈에 띄는 몇 개만 집으면 안 됩니다. `measured.short_lead_ratio`가 낮아도 순회는 그대로 합니다 — 그 값은 심각도 참고선이지 필터가 아닙니다.

**판정 결과를 쌍 단위로 남깁니다.** `review_required`의 `reporting`에 따라 삭제 결과, 문장 관계, 재구성문, 채택 또는 유지 이유를 기록합니다. 관계가 없는 쌍도 그 이유를 남깁니다. 관계가 있는 쌍은 최종적으로 유지하더라도 재구성문을 생략하지 않습니다. “독립 정보가 있다” 또는 “읽어보니 자연스럽다”만으로 유지하지 않습니다.

구문 검사에서도 **되돌린 문장을 실제로 적습니다.** 되돌려 보지 않고 "자연스럽다"로 넘기지 않습니다. 그리고 분열문은 한국어에도 있는 정상 구문이라 **전부 풀지 않습니다** — 문서에 한두 번 남는 것은 강조로 기능합니다.

대상으로 판정한 쌍은 **고친 문장을 실제로 보여줍니다.** 처방 문구만 전달하면 소비자 모델이 "연결어미로 잇기"로 좁게 해석해 결론 선행이 그대로 남는 문장을 만듭니다. 변환은 세 유형이고, 앞 문장이 통째로 사라지는 경우가 더 많습니다.

| 유형 | 원문 | 고친 문장 |
|---|---|---|
| 서술어로 이동 | 시작은 중국이었습니다. 상하이의 국영 기업이 DUV 노광 장비를 자체 개발해 양산에 들어갔다는 보도가 나왔고... | 상하이의 국영 기업이 DUV 노광 장비를 자체 개발해 양산에 들어갔다는 보도가 나오면서 **중국에서 시작됐습니다.** |
| 부사어로 흡수 | 방향이 완전히 무너진 건 수요일입니다. 다우가 2.19% 빠지면서 4대 지수가 전부 내렸고... | **수요일에는** 다우가 2.19% 빠지면서 4대 지수가 전부 내렸고... |
| 주어로 복원 | 목요일부터 시장을 되돌린 건 결국 실적이었습니다. | **목요일부터는 실적이** 시장을 되돌렸습니다. |

셋 다 안 맞을 때만 연결어미로 잇습니다. 그리고 **전부 고치지 않습니다.** 선언 구문이 0이 되면 과교정이며, 문단이 무엇을 다루는지 알려주는 기능까지 사라집니다.

### 권장 수정 방향 작성 원칙

원문 의미를 보존합니다. 정보를 빼거나 새 정보를 더하지 않고 자연스러운 한국어 표현으로만 옮깁니다.

일상 한국어로 풉니다. 매거진투, 보고서투, AI투 표현 자리에 일상 한국어가 들어갑니다. 정중한 평어체(`~합니다`, `~입니다`)가 기본이지만 원문이 친근체(`~예요`, `~네요`)였다면 친근체를 유지합니다.

한 문장에 대해 대안 1~2개를 제시합니다. 3개 이상은 사용자 결정 비용을 늘립니다. 개수를 채우려고 억지 대안을 만들지 않습니다 — 확신할 수 있는 수정문이 하나뿐이면 하나만 제시하고, 원문의 의미 해석이 실제로 둘로 갈릴 때만 두 개를 제시합니다.

매칭된 정규식은 사용자에게 한국어로 풀어 설명합니다.

### 코퍼스 룩업 결과 가공 (시그널 5 활성 시)

`awkward_collocations` 필드가 비어있지 않으면 사용자 보고에 함께 노출합니다. 형식은 다음과 같습니다.

```
- 코퍼스 룩업: "({명사}, {동사})" 자연 코퍼스 등장 {freq}건 — {깨진 의미 관계를 한국어로 진단}
```

진단은 단어 후보가 아니라 관계 설명입니다. 예: "'압박'을 '밀다'가 수식하면서 누가 무엇을 압박하는지가 불분명합니다. 수식어만 바꾸지 말고 행위자와 대상을 풀어 쓰세요."

`corpus_associations`는 사용자 보고에 노출하지 않습니다. 문맥을 보지 않은 빈도 연관어라 원문 의미와 무관한 단어가 섞여 있고, 이를 "자연 대안"처럼 보여주면 사용자와 후속 수정 LLM의 판단을 잘못된 방향으로 끌 수 있습니다. 적발이 타당한지 검증할 때(이 명사가 코퍼스에서 실제로 어떤 결합으로 쓰였는지 확인)만 내부적으로 참고합니다.

권장 수정 표현은 코퍼스 후보의 치환이 아니라, 원문이 전달하려는 의미를 한 줄로 고정한 뒤 그 의미를 유지하는 문장을 LLM이 일상 한국어로 새로 짜서 만듭니다. 필요하면 명사구를 서술 구조로 풀거나 주어·목적어를 다시 세우는 구조 수정을 단어 교체보다 우선합니다.

**시그널 5(코퍼스 룩업) 0건의 해석**: Leipzig 1M 코퍼스도 뉴스 도메인 규모 한계로 자연스러운 결합이 0건으로 나올 수 있습니다(예: 음성+담, 냉장고+남 같은 일상 결합). 의심 점수가 12점만으로 임계(17)를 넘지 못하면(나머지 시그널과 합산 17 미만) 자동으로 의심 목록에서 빠집니다. 그러나 임계를 넘어 의심으로 분류된 freq0 페어를 **"거짓 양성"으로 간주해 임의로 생략하지 않습니다.** freq0은 코퍼스 기준 '양성(적발)'이고, '거짓 양성'은 검사 결과가 아니라 LLM이 사후에 붙이는 미검증 라벨일 뿐입니다. 생략하려면 먼저 그 명사의 의미를 직접(자기 지식, 필요시 웹 검색) 확인하고, 그 명사에 자연스러운 동사인지를 같은 부류 명사의 연어로 판정해야 합니다. 동사가 어긋나면 적절한 동사로 바꾸고, 정말 자연스러우면 그 치환·의미 비교 근거를 한 줄 남긴 뒤에만 생략합니다. '코퍼스가 작아서/명사가 없어서 0' 단독 사유로는 생략할 수 없습니다. 이 판정 규칙은 스크립트 JSON 출력의 `signal_meaning` 필드로도 함께 전달됩니다.

**`oov_nouns`(코퍼스 부재 명사)의 처리**: 시그널 5는 코퍼스에 없는 명사를 만나면 판정 불가로 침묵합니다. 이 명사들은 JSON 최상위 `oov_nouns`에 모여서 나옵니다. 코퍼스가 2022년 뉴스 기준이므로 그 이후 신조어·상품명·고유명사가 주로 여기 잡힙니다. 이 명사들이 낀 문장은 **s5 미검수 상태**이므로, 본문 품질이 중요한 파이프라인에서는 oov 명사가 포함된 문장에 한해 LLM이 자기 지식과 웹 검색으로 명사의 의미를 확인하고 동사 결합이 자연스러운지 한 번 더 판정합니다. 검색으로도 확인이 안 되는 명사는 그 사실을 보고에 남깁니다. 이렇게 하면 "freq=0 적발"과 "아예 미검수" 두 사각지대가 모두 외부 검증으로 커버됩니다.

| 매칭 정규식 | 사용자에게 보여줄 표현 |
|-------------|------------------------|
| `r"가지고\s*있"` | "가지고 있" 표현 |
| `r"것으로\s*[보평확나사]"` | "것으로 보입니다/평가됩니다" 류 표현 |
| `r"는\s*점에서"` | "~는 점에서" 명사화 |
| `r"측면에서"` | "측면에서" 보고서투 |
| `r"본\s*제품"` | "본 제품" 매거진투 |

<example>

**입력**

`/tmp/knc_input.md` 파일 내용:

```
이 키보드는 우수한 타이핑감을 가지고 있다는 점에서 주목할 만합니다. 사용자에게 있어서 만족도가 높은 것으로 평가되고 있습니다.
```

**3단계 스크립트 실행 결과 (JSON)**

```json
{
  "total_sentences": 2,
  "suspect_count": 2,
  "average_score": 38.0,
  "suspects": [
    {
      "sentence": "이 키보드는 우수한 타이핑감을 가지고 있다는 점에서 주목할 만합니다.",
      "score": 40.0,
      "matched": ["가지고\\s*있", "주목할\\s*만", "는\\s*점에서"]
    },
    {
      "sentence": "사용자에게 있어서 만족도가 높은 것으로 평가되고 있습니다.",
      "score": 36.0,
      "matched": ["평가되[고는었]?", "것으로\\s*[보평확나사]", "것으로\\s*평가"]
    }
  ]
}
```

**4단계 사용자 보고 출력**

```
## 한국어 자연스러움 평가

검사 문장 2개 중 의심 문장 2개 (평균 점수 38.0)

### 의심 문장 1 (점수 40.0)
> 이 키보드는 우수한 타이핑감을 가지고 있다는 점에서 주목할 만합니다.
- 어색 신호: "가지고 있" 표현, "주목할 만" 매거진투, "~는 점에서" 명사화
- 권장 수정:
  - "이 키보드는 타이핑감이 정말 좋습니다"
  - "이 키보드는 타이핑감이 만족스럽습니다"

### 의심 문장 2 (점수 36.0)
> 사용자에게 있어서 만족도가 높은 것으로 평가되고 있습니다.
- 어색 신호: "있어서" 보고서투, "것으로 평가" 수동태 매거진투
- 권장 수정:
  - "사용자 만족도가 높습니다"

수정 후 다시 평가받으시면 됩니다.
```

</example>

### 의심 문장 0건일 때

스크립트가 의심 문장을 0건 반환하면 짧게 보고합니다. 도구의 검출 한계도 함께 안내해 사용자가 OK 결과를 과신하지 않도록 합니다.

<example>

**3단계 스크립트 실행 결과**

`{"total_sentences": 5, "suspect_count": 0, "average_score": 1.5, "suspects": []}`

**4단계 사용자 보고 출력**

```
## 한국어 자연스러움 평가

검사 문장 5개 중 의심 문장 0개 (평균 점수 1.5)

등록된 패턴과 코퍼스 결합 신호 범위에서는 뚜렷한 이상이 발견되지 않았습니다.

다만 이 검사의 재현율은 100%가 아니라서 경계선의 어색 문형은 통과할 수 있고, 문장 간 흐름·정보 배열 같은 문맥 자연스러움은 검사 범위 밖입니다. 한 번 직접 읽어보시면 도구가 놓친 부분을 잡을 수 있습니다.
```

</example>

### 의심 문장이 많을 때

20건 이상 의심 문장이 검출되면 상위 10건만 권장 수정 방향과 함께 자세히 보고하고, 나머지는 점수 순으로 한 줄씩 요약합니다. 한 응답에 30건 이상의 수정 사항을 보면 사용자가 어디부터 손대야 할지 막막해집니다. 상위 10건을 먼저 다듬고 재평가받는 사이클이 글 품질을 빠르게 끌어올립니다.
