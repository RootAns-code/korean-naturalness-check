---
name: korean-naturalness-check
version: 2.3.0
description: Use this skill whenever the user wants to evaluate the naturalness of a Korean blog post or any Korean text — checking for translation-style awkwardness (번역투), magazine-formal tone (매거진투), AI-typical phrasings (AI투), or stiff bureaucratic prose (직역체). Trigger on phrases like "한국어 평가", "자연스러움 검수", "어색한지 봐줘", "이 글 검수", "번역투 점검", "이 본문 평가해줘", or when the user gives a Korean .md file or pastes Korean text and asks for review/checking. Also trigger right after another blog-writing skill (naver-blog-studio-v2, naver-blog-review-writer, naver-shopping-connect 등) finishes producing a draft and the user asks for evaluation. Use this skill instead of attempting naturalness review yourself — LLMs have a known bias toward rating their own output as natural, and this skill provides external statistical signal that catches what self-review misses.
---

# 한국어 자연스러움 평가 스킬

## 스킬 목적

LLM이 자기가 쓴 한국어 본문을 자기가 검수하면 자연스럽다고 판정하는 편향이 있습니다. 이 편향 때문에 매거진투, 번역투, AI투 같은 어색한 표현이 자체 검수에서 통과돼 사용자에게 그대로 전달되는 사례가 누적됐습니다.

이 스킬은 형태소 분석기(`kiwipiepy`), 어색 어휘 사전, 그리고 한국어 자연 코퍼스(Leipzig kor_news_2022_1M) 기반 명사+동사 결합 빈도 룩업을 결합한 정량 검수 도구를 통해 LLM 자체 검수와 다른 분포의 외부 시그널을 제공합니다. 코퍼스 결합 빈도 룩업은 사전에 등록되지 않은 어색 표현(예: "기업을 묶다", "사례를 묶음으로")도 자동 검출합니다. 이 스킬의 책임은 의심 문장을 검출하고 권장 수정 방향을 제시하는 것까지입니다. 본문 자동 수정은 글 작성 스킬이나 사용자 후속 지시에서 처리합니다.

<workflow>

## 워크플로우 4단계

1단계 입력 정규화: 사용자 입력을 파일 경로로 통일
2단계 의존성 확인: `kiwipiepy` 설치 여부 확인
3단계 검수 스크립트 실행: `scripts/check_naturalness.py --json`
4단계 사용자 보고: JSON 결과를 가공해 한국어 보고

각 단계는 이전 단계 결과 위에 쌓이므로 순서대로 진행합니다.

</workflow>

## 1단계: 입력 정규화

검수 스크립트는 마크다운 파일 경로를 입력으로 받습니다. 사용자가 어떤 형태로 본문을 제공하든 파일 경로로 통일한 뒤 다음 단계로 갑니다.

<input_handling>

**파일 경로로 받은 경우**: 사용자가 `/mnt/user-data/outputs/foo.md` 같은 경로를 주거나, 직전에 다른 스킬이 만든 산출물의 위치를 가리킬 때 그 경로를 그대로 사용합니다.

**인라인 텍스트로 받은 경우**: 사용자가 채팅창에 본문을 붙여넣고 평가를 요청할 때 텍스트를 `/tmp/knc_input.md`에 저장한 뒤 그 경로를 사용합니다.

```bash
cat > /tmp/knc_input.md << 'EOF'
[사용자가 제공한 본문 전체]
EOF
```

**입력이 모호한 경우**: 사용자가 "이 글 평가해줘"라고만 하고 본문 위치를 명시하지 않으면 한 번 묻고 진행합니다. 추측해서 임의 파일을 검수하지 않습니다. 잘못된 파일을 검수하면 사용자가 결과를 보고도 해당 파일과 매칭하기 어려워 혼란이 커집니다.

</input_handling>

## 2단계: 의존성 확인

`scripts/check_naturalness.py`는 한국어 형태소 분석기 `kiwipiepy`가 필요합니다. 처음 실행 시 설치합니다.

```bash
pip install --break-system-packages kiwipiepy 2>&1 | tail -3
```

이미 설치돼 있으면 즉시 종료합니다. Claude.ai 웹 컨테이너와 PC 환경 양쪽에서 설치 가능 검증됨.

## 3단계: 검수 스크립트 실행

JSON 모드로 실행해 후처리 가능한 형태로 결과를 받습니다. 텍스트 모드 출력은 사람이 바로 읽기 좋게 정리되지만, 4단계에서 LLM이 한국어 감각으로 추가 가공하려면 JSON이 더 적합합니다.

```bash
python3 scripts/check_naturalness.py <파일 경로> --json
```

스크립트가 반환하는 JSON 구조는 다음과 같습니다.

```json
{
  "file": "...",
  "total_sentences": 8,
  "suspect_count": 3,
  "average_score": 22.4,
  "threshold": 17,
  "corpus_signal_active": true,
  "signal_meaning": "corpus_collocation freq=0은 코퍼스 기준 양성이다. '거짓양성'은 모델이 붙이는 미검증 라벨일 뿐이므로, 명사 의미를 확인해 동사 적합성을 판정하기 전엔 일축 금지 (전문은 실제 출력 참조)",
  "oov_nouns": ["닌텐도", "스위치"],
  "suspects": [
    {
      "sentence": "본문 문장",
      "score": 60.0,
      "breakdown": {
        "noun_density": 0,
        "noun_verb_ratio": 20,
        "bound_josa_chains": 0,
        "awkward_lexical": 40,
        "corpus_collocation": 0
      },
      "matched": ["측면에서", "것으로\\s*[보평확나사판알전풀]"],
      "matched_groups": ["보고서투_명사구", "것으로_판단동사"],
      "awkward_collocations": [],
      "oov_nouns": []
    },
    {
      "sentence": "분야별 관심 기업이 다음과 같이 묶입니다.",
      "score": 25.3,
      "breakdown": {
        "noun_density": 5.3,
        "noun_verb_ratio": 8.0,
        "bound_josa_chains": 0,
        "awkward_lexical": 0,
        "corpus_collocation": 12
      },
      "matched": [],
      "matched_groups": [],
      "awkward_collocations": [
        {
          "noun": "기업",
          "verb": "묶이",
          "freq": 0,
          "sig": 0.0,
          "alternatives": [
            {"verb": "거듭나", "freq": 49, "sig": 343.9},
            {"verb": "늘", "freq": 84, "sig": 239.0},
            {"verb": "가", "freq": 65, "sig": 102.5}
          ]
        }
      ],
      "oov_nouns": []
    }
  ],
  "all_results": [...]
}
```

`oov_nouns`(최상위·문장별)는 코퍼스에 아예 없어 시그널 5가 판정 불가로 침묵한 명사 목록입니다. 코퍼스 기준 시점(2022) 이후 신조어·고유명사가 주로 여기에 잡히며, 이 명사들의 결합은 **검사를 통과한 것이 아니라 검사를 받지 않은 것**입니다.

`corpus_signal_active`가 `false`로 나오면 동봉 코퍼스 DB(`assets/kor_collocation.db`)가 누락된 환경입니다. 이 경우 시그널 5는 비활성이지만 1~4번 시그널은 정상 동작합니다. 사용자 보고 시 코퍼스 룩업 결과 없이 진행하면 됩니다.

## 4단계: 사용자 보고

JSON 결과를 가공해 한국어로 보고합니다. 정규식 패턴은 사용자에게 정보 가치가 없습니다. 사용자는 어떤 문장이 어색하고 어떻게 다듬을지를 알고 싶어합니다. 그래서 LLM이 정규식을 한국어 설명으로 옮기고, 자연스러운 대안을 1~2개 제시합니다.

<output_format>

다음 형식으로 보고합니다.

```
## 한국어 자연스러움 평가

검사 문장 N개 중 의심 문장 M개 (평균 점수 X.X)

### 의심 문장 1 (점수 60.0)
> [원문 문장]
- 어색 신호: [matched 패턴을 한국어로 풀어서 설명]
- 권장 수정: [자연스러운 대안 1~2개]

### 의심 문장 2 (점수 40.0)
> ...

수정 후 다시 평가받으시면 됩니다.
```

</output_format>

### 권장 수정 방향 작성 원칙

원문 의미를 보존합니다. 정보를 빼거나 새 정보를 더하지 않고 자연스러운 한국어 표현으로만 옮깁니다.

일상 한국어로 풉니다. 매거진투, 보고서투, AI투 표현 자리에 일상 한국어가 들어갑니다. 정중한 평어체(`~합니다`, `~입니다`)가 기본이지만 원문이 친근체(`~예요`, `~네요`)였다면 친근체를 유지합니다.

한 문장에 대해 대안 1~2개를 제시합니다. 3개 이상은 사용자 결정 비용을 늘립니다.

매칭된 정규식은 사용자에게 한국어로 풀어 설명합니다.

### 코퍼스 룩업 결과 가공 (시그널 5 활성 시)

`awkward_collocations` 필드가 비어있지 않으면 사용자 보고에 함께 노출합니다. 형식은 다음과 같습니다.

```
- 코퍼스 룩업: "({명사}, {동사})" 자연 코퍼스 등장 {freq}건
  같은 명사로 자주 쓰이는 자연 대안: {alternatives 상위 2~3개를 한국어 동사 활용형으로 풀어서}
```

권장 수정에 코퍼스 대안을 직접 박지 않습니다. 대안 동사는 어간이므로 활용형 처리가 필요하고, 문맥 적합성은 LLM이 한국어 감각으로 최종 판단해야 합니다. 코퍼스 결과는 사용자에게 어색함의 근거로 제시하고 권장 수정 표현은 LLM이 일상 한국어로 새로 짭니다.

**시그널 5(코퍼스 룩업) 0건의 해석**: Leipzig 1M 코퍼스도 뉴스 도메인 규모 한계로 자연스러운 결합이 0건으로 나올 수 있습니다(예: 음성+담, 냉장고+남 같은 일상 결합). 의심 점수가 12점만으로 임계(17)를 넘지 못하면(나머지 시그널과 합산 17 미만) 자동으로 의심 목록에서 빠집니다. 그러나 임계를 넘어 의심으로 분류된 freq0 페어를 **"거짓 양성"으로 간주해 임의로 생략하지 않습니다.** freq0은 코퍼스 기준 '양성(적발)'이고, '거짓 양성'은 검사 결과가 아니라 LLM이 사후에 붙이는 미검증 라벨일 뿐입니다. 생략하려면 먼저 그 명사의 의미를 직접(자기 지식, 필요시 웹 검색) 확인하고, 그 명사에 자연스러운 동사인지를 같은 부류 명사의 연어로 판정해야 합니다. 동사가 어긋나면 적절한 동사로 바꾸고, 정말 자연스러우면 그 치환·의미 비교 근거를 한 줄 남긴 뒤에만 생략합니다. '코퍼스가 작아서/명사가 없어서 0' 단독 사유로는 생략할 수 없습니다. 이 판정 규칙은 스크립트 JSON 출력의 `signal_meaning` 필드로도 함께 전달됩니다.

**`oov_nouns`(코퍼스 부재 명사)의 처리**: 시그널 5는 코퍼스에 없는 명사를 만나면 판정 불가로 침묵합니다. 이 명사들은 JSON 최상위 `oov_nouns`에 모여서 나옵니다. 코퍼스가 2022년 뉴스 기준이므로 그 이후 신조어·상품명·고유명사가 주로 여기 잡힙니다. 이 명사들이 낀 문장은 **s5 미검수 상태**이므로, 본문 품질이 중요한 파이프라인에서는 oov 명사가 포함된 문장에 한해 LLM이 자기 지식과 웹 검색으로 명사의 의미를 확인하고 동사 결합이 자연스러운지 한 번 더 판정합니다. 검색으로도 확인이 안 되는 명사는 그 사실을 보고에 남깁니다. 이렇게 하면 "freq=0 적발"과 "아예 미검수" 두 사각지대가 모두 외부 검증으로 커버됩니다.

| 매칭 정규식 | 사용자에게 보여줄 표현 |
|-------------|------------------------|
| `r"가지고\s*있"` | "가지고 있" 표현 |
| `r"것으로\s*[보평확나사]"` | "것으로 보입니다/평가됩니다" 류 표현 |
| `r"는\s*점에서"` | "~는 점에서" 명사화 |
| `r"측면에서"` | "측면에서" 보고서투 |
| `r"본\s*제품"` | "본 제품" 매거진투 |

<example>

**입력**

`/tmp/knc_input.md` 파일 내용:

```
이 키보드는 우수한 타이핑감을 가지고 있다는 점에서 주목할 만합니다. 사용자에게 있어서 만족도가 높은 것으로 평가되고 있습니다.
```

**3단계 스크립트 실행 결과 (JSON)**

```json
{
  "total_sentences": 2,
  "suspect_count": 2,
  "average_score": 38.0,
  "suspects": [
    {
      "sentence": "이 키보드는 우수한 타이핑감을 가지고 있다는 점에서 주목할 만합니다.",
      "score": 40.0,
      "matched": ["가지고\\s*있", "주목할\\s*만", "는\\s*점에서"]
    },
    {
      "sentence": "사용자에게 있어서 만족도가 높은 것으로 평가되고 있습니다.",
      "score": 36.0,
      "matched": ["평가되[고는었]?", "것으로\\s*[보평확나사]", "것으로\\s*평가"]
    }
  ]
}
```

**4단계 사용자 보고 출력**

```
## 한국어 자연스러움 평가

검사 문장 2개 중 의심 문장 2개 (평균 점수 38.0)

### 의심 문장 1 (점수 40.0)
> 이 키보드는 우수한 타이핑감을 가지고 있다는 점에서 주목할 만합니다.
- 어색 신호: "가지고 있" 표현, "주목할 만" 매거진투, "~는 점에서" 명사화
- 권장 수정:
  - "이 키보드는 타이핑감이 정말 좋습니다"
  - "이 키보드는 타이핑감이 만족스럽습니다"

### 의심 문장 2 (점수 36.0)
> 사용자에게 있어서 만족도가 높은 것으로 평가되고 있습니다.
- 어색 신호: "있어서" 보고서투, "것으로 평가" 수동태 매거진투
- 권장 수정:
  - "사용자 만족도가 높습니다"
  - "사용자들이 만족스러워합니다"

수정 후 다시 평가받으시면 됩니다.
```

</example>

### 의심 문장 0건일 때

스크립트가 의심 문장을 0건 반환하면 짧게 보고합니다. 도구의 검출 한계도 함께 안내해 사용자가 OK 결과를 과신하지 않도록 합니다.

<example>

**3단계 스크립트 실행 결과**

`{"total_sentences": 5, "suspect_count": 0, "average_score": 1.5, "suspects": []}`

**4단계 사용자 보고 출력**

```
## 한국어 자연스러움 평가

검사 문장 5개 중 의심 문장 0개 (평균 점수 1.5)

본문이 자연스러운 한국어 분포 안에 있습니다.

도구는 사전에 등록된 패턴 위주로 검출하므로, 사전에 없는 새로운 어색 표현은 놓칠 수 있습니다. 한 번 직접 읽어보시면 도구가 놓친 부분을 잡을 수 있습니다.
```

</example>

### 의심 문장이 많을 때

20건 이상 의심 문장이 검출되면 상위 10건만 권장 수정 방향과 함께 자세히 보고하고, 나머지는 점수 순으로 한 줄씩 요약합니다. 한 응답에 30건 이상의 수정 사항을 보면 사용자가 어디부터 손대야 할지 막막해집니다. 상위 10건을 먼저 다듬고 재평가받는 사이클이 글 품질을 빠르게 끌어올립니다.

## 도구의 검증 정확도와 한계

`eval/run_eval.py` 라벨셋(자연 70문장·어색 72문장, 2.3.0 기준) 실측:

- 어색 문장 재현율 86.1% (62/72)
- 자연 문장 거짓양성률 10.0% (7/70)
- 정밀도 89.9%
- 자연 평균 7.2점 vs 어색 평균 26.8점 (격차 +19.6)

해석 시 유의점 두 가지입니다.

**의심 보고가 항상 옳은 것은 아닙니다.** 남은 거짓양성은 대부분 뉴스 코퍼스에 일상 결합이 없어 생기는 s5 적발(예: 냉장고+남다, 텃밭+심다)입니다. 그래서 s5 단독 근거(어휘 패턴 매칭 없이 `corpus_collocation`만으로 임계를 넘은 문장)는 위의 "0건의 해석" 절차대로 명사 의미를 확인한 뒤 보고합니다. 반대로 `matched`에 확실 신호(이중수동, "~에 있어서" 등)가 잡힌 문장은 실제로 어색할 확률이 매우 높습니다.

**OK 판정도 완전하지 않습니다.** 재현율 86%가 의미하듯 12점 경계선의 어색 문형(단일 수동 한자어, 명사화 '의' 단독 등)은 통과할 수 있습니다. 사용자에게 OK 결과를 보고할 때 이 한계를 함께 안내합니다.

시그널·가중치·임계값·사전을 수정할 때는 반드시 `python3 eval/run_eval.py`로 전후 수치를 비교합니다. 라벨셋은 초기 부트스트랩 세트이므로, 실사용에서 발견되는 거짓양성/거짓음성 문장을 라벨셋에 추가할수록 측정이 정확해집니다.

## 어색 패턴 사전 누적

시그널 5(코퍼스 룩업) 덕분에 정규식 사전에 등록되지 않은 어색 결합도 자동 검출됩니다. 다만 코퍼스가 못 잡는 표현(매거진 명사구, 보고서투 명사화 등 결합 단위가 아닌 패턴)은 여전히 정규식 사전에 누적합니다.

사전은 `scripts/check_naturalness.py`의 `AWKWARD_PATTERN_GROUPS`에 현상 단위 그룹으로 관리합니다. 같은 현상을 겨누는 패턴들이 한 그룹이고, 한 문장에서 그룹당 1회만 가산합니다("것으로 평가되고" 하나가 패턴 3개에 겹쳐 3배 과금되는 것을 막기 위함). 그룹 가중치는 두 단계입니다.

- **12점(강한 신호)**: 자연 한국어에서 거의 안 나오는 문형. 예: 이중수동("되어집니다"), "~에 있어서".
- **6점(약한 신호)**: 자연 용례도 흔한 문체 경향. 예: "의 경우", "것이다", "에 따라". 단독으로는 임계(17)를 못 넘고 다른 신호와 합쳐질 때만 의심으로 분류되게 하여 자연 문장 거짓양성을 막습니다.

새로운 어색 표현을 발견하면 해당하는 그룹에 정규식을 추가하거나, 새 현상이면 새 그룹을 만듭니다. 활용 어미는 `[가나다]` 형태로 묶어 흡수하되, **융합 음절을 빠뜨리지 않도록** 실제 활용형을 나열해보고 문자클래스를 정합니다.

<example>

**좋은 패턴 추가 예시**

```python
r"판단되어[지진질집짐졌]"
```

이 한 줄로 다음 활용형을 모두 잡습니다.

- 판단되어진다 / 판단되어졌다 / 판단되어지고
- 판단되어**집**니다, 판단되어**질**, 판단되어**짐** (융합 음절)

**나쁜 패턴 추가 예시**

```python
r"판단되어[지진졌]"
```

문자클래스에 융합 음절(집/질/짐)이 빠지면 "판단되어집니다", "판단되어질"을 놓칩니다. 실제로 2.2.0까지 이 형태의 클래스 때문에 이중수동 상당수가 통과했습니다(2.3.0에서 수정).

</example>

패턴을 추가·조정한 뒤에는 반드시 `python3 eval/run_eval.py`로 정밀도·재현율 회귀 여부를 실측합니다.

## 동봉 코퍼스 데이터 관리

`assets/kor_collocation.db`는 Leipzig kor_news_2022_1M 패키지(CC BY 4.0)의 **원문 100만 문장**을 kiwipiepy로 형태소 분석해 빌드한 명사+동사 결합 빈도 SQLite DB입니다(163,272페어, 명사 19,284종, 동사 2,258종, 약 12.5MB). 스킬에 동봉되어 모든 사용자(Claude.ai 웹, PC Claude Code 양쪽)가 별도 셋업 없이 즉시 활용 가능합니다.

빌드 파이프라인의 핵심 원칙은 **DB와 런타임이 같은 추출기를 쓴다**는 것입니다. `build_collocation_data.py`는 `check_naturalness.py`의 `_extract_noun_verb_pairs`를 그대로 임포트해 코퍼스를 집계하므로, 런타임이 조회하는 페어 분포와 DB의 페어 분포가 정의상 일치합니다(2.x 시절 인접 어절 공기표 기반 DB가 "사진을 예쁘게 찍다" 같은 비인접 자연 결합을 0건 거짓양성으로 만들던 구조적 불일치를 차단). 따라서 **추출기 규칙을 수정하면 반드시 DB를 재빌드합니다.** 유의도는 Dunning 로그우도(LLR)를 자체 계산하며, DB의 `build_info` 테이블에 빌드 출처·kiwipiepy 버전이 기록됩니다.

코퍼스를 교체하려면 `scripts/build_collocation_data.py`의 `URL` 상수를 바꾼 뒤 스킬 개발자 환경에서 1회 재빌드합니다(1M 문장 기준 약 2분). 현재 1M이 Leipzig 한국어 뉴스 코퍼스의 최대 규모이며, 2022년판이 최신입니다(2023년 이후판 미제공, 2026-06 확인). 남은 확장 방향은 뉴스 외 도메인(블로그·구어) 코퍼스 추가입니다 — 남은 거짓양성 대부분이 뉴스에 안 나오는 일상 결합이기 때문입니다. 후보 선정 시 웹 크롤 텍스트는 기계번역체 오염 위험이 있어 편집된 한국어(국립국어원 모두의 말뭉치 등)를 우선합니다. 복수 코퍼스를 쓸 때는 "모든 코퍼스에서 0건"일 때만 적발하는 교집합 방식이 거짓양성을 줄입니다.

빌드 스크립트는 사용자가 실행할 필요가 없습니다. 스킬 패키지가 이미 빌드된 DB를 가지고 있기 때문입니다. 빌드 스크립트는 향후 코퍼스 갱신·확장을 위해 스킬 패키지에 포함되어 있습니다.

라이선스 표기: Leipzig Corpora Collection — kor_news_2022_1M, CC BY 4.0, https://wortschatz.uni-leipzig.de/en/download/Korean

## 다른 글 작성 스킬과의 관계

이 스킬은 글 작성 스킬과 직교적입니다. 글 작성 스킬이 본문을 만들고 산출물을 저장한 직후, 사용자가 이 스킬을 호출해 평가받는 흐름입니다.

| 글 작성 스킬 | 평가 호출 시점 |
|--------------|----------------|
| `naver-blog-studio-v2` | 본문 작성 완료, 산출물 저장 직후 |
| `naver-blog-review-writer` | 본문 작성 완료, 사용자에게 보여준 직후 |
| `naver-shopping-connect` | 본문 작성 완료, 산출물 저장 직후 |

평가 후 사용자가 결과를 보고 글 작성 스킬에 "이 부분 다시 다듬어줘"라고 지시하는 흐름입니다. 이 스킬에서 본문 자동 수정은 하지 않습니다. 평가와 수정을 분리하는 이유는 평가 도구의 객관성을 유지하기 위해서입니다. 평가하는 LLM이 수정도 하면 자기가 다듬은 표현에 대해 다시 자기가 평가하는 자체 검수 편향이 발생합니다.

## 버전 관리

이 스킬은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다. 변경 이력은 `CHANGELOG.md`에 기록합니다.

- MAJOR: 워크플로우 단계 자체가 바뀌거나 입력/출력 형식이 호환 깨짐
- MINOR: 새 시그널 추가, 새 어휘 사전 카테고리 추가, 출력 항목 확장
- PATCH: 어휘 사전에 패턴 추가, 임계값 미세 조정, 표현 다듬기

스킬을 수정할 때 `SKILL.md` 프론트매터의 `version`과 `CHANGELOG.md`를 함께 갱신합니다.
