# Changelog

이 스킬의 변경 이력을 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식으로 기록합니다.

## [2.7.0] - 2026-08-02

문장 단위 시그널이 구조상 볼 수 없는 **문단 설계 층위**를 관측 항목으로 추가했습니다. 문장 점수·임계값·어휘 사전은 변경 없음. 사용자가 "이어 써도 될 문장을 너무 끊어 쓴다", "쓸데없는 수식어가 많다"고 지적한 것이 배경이며, 실산출물 18편을 측정해 원인을 특정했습니다.

측정에서 드러난 것: 어휘 층위 번역투(이중피동·이중조사·have/make 직역·연결어미 뒤 쉼표·한자어 명사화)는 **전부 0건**이었고, 남은 어색함이 "문단 첫머리에 짧은 명사서술 선언을 놓고 뒤에 설명을 붙이는" 구조에 몰려 있었습니다(한 글에서 29문단 중 10건). 이 구조가 문장을 끊고, 짧아진 문장에 정보를 넣으려다 수식이 붙습니다. 사용자가 직접 편집한 글에서 문장 65→53개, 절 연결 31%→40%로 움직인 흔적이 이 진단을 뒷받침합니다.

### Added

- **JSON `document_patterns` 필드**: `diagnosis`(처방 대상) / `confirm_only`(검증 지표) / `meaning`(소비자 지시문) 구조. `review_band_meaning`과 같은 방식으로 SKILL.md를 읽지 않는 파이프라인에도 처리 지시가 전달됨
- **`short_declarative_lead` 진단**: 2문장 이상 문단의 첫 문장이 25자 미만 + 명사서술 종결인 경우. 처방은 "삭제"가 아니라 "앞 문장의 정보를 뒤 문장의 주어·부사어·서술어 자리로 옮겨 한 문장으로 다시 쓰기"이며, 연결어미만 붙이면 결론 선행이 남아 더 어색해진다는 단서와 전멸 금지 경고를 포함
- **검증 지표 `clause_link_rate` / `long_sentence_count`**: 값만 싣고 "직접 겨냥 금지"를 명시. 겨냥하면 절 경계 쉼표와 수식어 증식을 부름
- **텍스트 리포트에 문단 설계 관측 블록 추가**

### Fixed

- **Windows cp949 콘솔에서 텍스트 모드가 죽던 문제**: `sys.stdout.reconfigure(encoding='utf-8')` 추가. 2.6.0이 추가한 재판단 밴드 줄의 em dash에서 `UnicodeEncodeError`로 중단됐고, 리다이렉트해도 stdout 인코딩이 cp949면 동일하게 실패했음

### 설계 결정

- **임계값을 두지 않음**: 문단 구조에는 라벨셋이 없어 임계의 근거를 만들 수 없고, 근거 없는 임계는 "미달이니 통과"로 모델이 뭉개는 부작용이 더 큼. 발동 통제는 "0건이면 항목을 싣지 않는다"로 대신함. 따라서 `eval/run_eval.py`의 실측 요구 대상이 아님
- **장르 플래그 대신 측정 성립 여부**: SNS 톤은 마침표를 생략해 장문 수치가 실제와 무관해지므로, 장르를 추정하는 대신 종결부호 밀도만 보고 `long_sentence_count`를 `null`로 침묵시킴. 오판해도 침묵할 뿐이라 안전. `short_declarative_lead`는 "2문장 이상 문단" 조건 덕분에 SNS에서 자연히 침묵함(실측 확인)

### 기각

- **좌향 관형구 중첩(A-18 계열)**: 도입 검토 중 실측으로 기각. 문장의 관형형 전성어미(ETM) 3개 이상 기준으로 18편에서 28건 적발됐으나 대부분 정상 문장이었고, 서술어 경계 없이 연속된 ETM만 세도록 좁혀 7건까지 줄여도 남은 7건이 전부 자연스러웠음("운동회나 콘서트장처럼 줌을 많이 쓰는 곳에서 촬영하는 분들은 체감이 클 수밖에 없어요"). 한국어는 head-final이라 관형절 중첩이 정상 문법이며, 원 규칙의 근거는 영어 관계대명사절 직역이라 자생 한국어 산문에는 발동 조건이 성립하지 않음. 재도입하려면 번역문 코퍼스로 먼저 검정할 것
- **문단당 문장 수(E-3 계열)**: 다수 프로젝트가 "1문단 1정보"를 모바일 가독성 지침으로 강제하고 있어, 문단당 2문장 편중은 병리가 아니라 의도된 설계임
- **동의어 이중 수식 / "이는 ~" 메타 진입 / 이중·삼중 완곡**: 18편 약 900문장에서 실질 적발 0건(H-3 2건·G-2 1건은 검토 결과 전부 오탐). 정규식 sanity check로 양성 샘플 10개 중 9개 적중을 확인해 검출기 결함이 아님을 배제함

### 검증

- `eval/run_eval.py` 전후 동일: 재현율 85.0%, 거짓양성 12.8%, 정밀도 87.2% (문장 점수 로직 무변경 확인)
- 블로그 산출물에서 적발·처방 출력, SNS 글과 잘 쓴 블로그 글에서 침묵 확인

## [2.6.0] - 2026-08-02

임계 미만 적발의 "미보고 사각지대"를 출력 자체에 동봉한 업데이트입니다. 검출 로직·점수·임계값은 변경 없음. 소비 스킬(draft-rewrite 등)이 "suspects를 처리한다"라고만 적어 두면 SKILL.md의 `--threshold 12` 재판단 밴드 권고가 전달되지 않아, 12점 단일 적발("추정이 모이다")이 미보고로 새고 사용자가 승인 게이트 이후 직접 잡아낸 사례가 배경입니다.

### Added

- **JSON `review_band` / `review_band_count` 필드**: 12점 이상 임계 미만 문장을 suspects와 같은 구조로 항상 동봉. `--threshold 12`로 실행하면 밴드가 suspects로 흡수되어 빈 배열
- **JSON `review_band_meaning` 필드**: `signal_meaning`과 같은 방식의 소비자 지시문 — "임계 아래는 통과가 아니라 미보고", 조각 적발 제외 후 판정, oov 문장 재확인, "suspects만 처리하면 검사를 절반만 쓴 것" 명시. SKILL.md를 읽지 않는 파이프라인 호출에서도 모델이 JSON만으로 지시를 받게 함
- **텍스트 리포트에 재판단 밴드 줄 추가**

### 검증

- 검출 로직 무변경(출력 필드 추가만). 스모크 테스트로 신규 필드 출력 확인

## [2.5.0] - 2026-07-21

코퍼스 연관어의 역할을 "대안 추천"에서 "적발 근거 자료"로 재정의한 보고 계층 업데이트입니다. 검출 로직·점수·임계값은 변경 없음(라벨셋 수치 동일). 외부 검토에서 받은 두 지적 — ① 통계 연관어를 "자연 대안"처럼 제시하면 문맥 무관 후보(기업+묶이 적발에 거듭나/늘)가 후속 수정 LLM의 방향을 오염시킴(앵커링) ② 의심 0건 문구("자연스러운 한국어 분포 안에 있습니다")가 재현율 85% 도구의 실제 성능보다 강한 확신을 줌 — 의 반영입니다.

### Changed

- **JSON 필드 개명(호환성 주의)**: `awkward_collocations[].alternatives` → `corpus_associations`. 값 구조는 동일하나 의미를 재정의 — 문장을 보지 않는 단어 빈도 통계이므로 대체 표현 후보가 아니라 "이 단어는 원래 이런 결합으로 쓰인다"는 적발 근거 자료. JSON을 파싱하는 소비자는 필드명을 갱신해야 합니다
- **사용자 보고에서 연관어 기본 미노출**: 코퍼스 룩업은 깨진 의미 관계의 진단문("누가 무엇을 압박하는지 불분명 — 행위자·대상을 풀어 쓸 것")으로 보고하고, 단어 후보 목록은 보여주지 않음. 텍스트 리포트의 "자연 대안:" 줄 제거
- **수정 원칙을 구조 우선으로 명문화**: 문제 단어 치환보다 원문 의미 고정 → 의미 관계 재구성(명사구→서술 구조, 주어·목적어 재수립)을 우선. `signal_meaning` 지침에도 동일 규칙 추가
- **억지 대안 금지**: 대안 1~2개 규칙에 "확신할 수 있는 수정문이 하나면 하나만, 의미 해석이 실제로 갈릴 때만 2개" 명시. SKILL.md 예시의 어색한 2번 대안("사용자들이 만족스러워합니다") 제거
- **의심 0건 보고 문구 완화**: "자연스러운 한국어 분포 안에 있습니다" → "등록된 패턴과 코퍼스 결합 신호 범위에서는 뚜렷한 이상이 발견되지 않았습니다" + 재현율 한계·문맥 검사 범위 밖 안내

### 검증

- `eval/run_eval.py` 전후 동일: 재현율 85.0%, 거짓양성 12.8%, 정밀도 87.2% (검출 로직 무변경 확인)

## [2.4.0] - 2026-07-20

코퍼스 검사(s5)를 명사→용언 단일 관계에서 **3종 결합 관계**로 확장한 마이너 업데이트입니다. "명사 동사 결합만 잡으면 중간에 들어가는 어색한 형용사·부사는 다 통과하느냐"는 사용자 지적(실산출물 "유료 전환을 미는 압박" 등에서 확인)이 배경입니다.

### Added

- **vn 관계(형용사 관형형→명사)**: "두꺼운 신뢰", "깊은 가격" 같은 수식 어색 검출. 형용사(VA/XSA)로 제한 — 일반 동사 관형형("산 신발")까지 검사하면 뉴스 코퍼스 희소성으로 자연 결합이 대량 거짓양성이 됨을 실측(28.2%)하고 범위를 좁힘
- **av 관계('-히'계 부사→용언)**: "급격히 진행했습니다", "활발히 해결했습니다" 류 보고서투 부사 남용 검출. 일상 부사(정말/바로/새로)는 자유 결합이라 제외
- **하다류 용언 지원(nv 확장)**: 명사·어근+XSV/XSA 파생 용언("발견하다", "따뜻하다")을 어간으로 결합 — 2.3까지는 하다동사가 통째로 검사 밖이었음. nv 결합 163,272 → 286,562개로 확대
- **JSON `rel` 필드**: `awkward_collocations` 각 항목에 결합 관계(nv/vn/av) 표기. `alternatives`는 관계에 맞는 자연 대안(vn이면 그 명사의 자연 수식어: 신뢰 → 변함없/두텁/깊)
- **정규식 그룹 `직역_명사구`(18점)**: "유료 전환을 미는 압박" — kiwi가 '미는'을 명사+조사로 오분석해 통계로는 원리상 못 잡는 부류의 정규식 차단
- **V+게 부사절 스킵 확장**: "나도 모르게"(VV+게)가 페어를 가로채던 사각 수정(기존 VA+게만 스킵)

### Changed

- **DB 재빌드(스키마 확장)**: `rel` 컬럼 추가, 관계별 LLR 자체 계산. 총 319,683결합(nv 286,562 / vn 28,590 / av 4,531), 12.5 → 36.5MB. 구(rel 없는) DB로도 nv 검사는 동작(하위 호환)
- **문장 분리를 줄 단위로 변경**: 마침표 없는 줄(SNS 캐주얼 톤)을 한 문장으로 취급 — 여러 줄이 한 덩어리로 합쳐져 형태소 통계가 왜곡되던 문제 방지
- **수식 적발의 임계 설계**: 단일 vn/av 적발은 12점으로 기본 임계(17) 미달 — 벌점을 임계 이상으로 올리면 (천천히,오르) 같은 자연-희소 결합이 단독 거짓양성이 됨을 실측으로 확인. 파이프라인 소비자는 `--threshold 12`로 실행해 12~16점의 vn/av 결합 문장을 재판단 밴드로 처리 권장 (라벨셋 기준 밴드 구성: 어색 4건 vs 자연 1건)

### 검증 (라벨셋 자연 78·어색 80문장, 수식 표적 16문장 추가)

- 임계 17: 재현율 85.0%, 거짓양성 12.8%, 정밀도 87.2%, 평균 격차 +22.5점
- 재판단 밴드(12~16 & vn/av) 포함 시 재현율 90.0%
- 2.3.0 대비: 수식 어색("두꺼운 신뢰", "급격히 진행", "무거운 화제", "강한 사용성")이 0점 통과 → 검출/밴드 진입. 자연 수식("따뜻한 커피", "두터운 신뢰")은 0점 유지

### 호환성

- 기존 JSON 키 유지, `rel`만 신규. 시그널 가중치·기본 임계(17)·워크플로우 동일
- `_extract_noun_verb_pairs`는 하위 호환 래퍼로 유지(nv만 반환)
- 추출기·DB가 세트로 바뀜 — 한쪽만 롤백 금지

## [2.3.0] - 2026-07-19

품질 검토에서 실측으로 확인된 결함들을 일괄 수정한 마이너 업데이트입니다. 라벨 평가셋 기준 **거짓양성 25.7% → 10.0%, 재현율 45.8% → 86.1%, 정밀도 64.7% → 89.9%** (`eval/baseline_2.2.0.txt` 참고). JSON 기존 키는 모두 유지되고 신규 필드만 추가됐습니다.

### 배경

2.2.0 검토에서 라벨셋(자연 70·어색 72문장)을 만들어 실측한 결과, 문서의 "자연 문장 거짓양성 0%" 주장과 달리 실제 거짓양성 25.7%, 재현율 45.8%였습니다. 주요 원인 네 가지: ① s5 페어 추출기가 부사화 형용사("예쁘게")·관용 부사구("다음과 같이")에 페어를 오부착, ② DB가 인접 어절 공기(co_n) 기반이라 런타임의 10형태소 거리 추출과 구조적 불일치, ③ 정규식 사전의 같은-현상 중복 가산(한 표현에 36점)과 융합 음절 누락("되어집니다"가 `[지진졌]`을 통과), ④ "의 경우" 같은 자연 용례 많은 패턴이 단독 12점으로 임계 근처까지 도달.

### Added

- **`eval/` 평가 하네스 신규**: 자연 70·어색 72문장 라벨셋 + `run_eval.py`(정밀도·재현율·거짓양성률·시그널 기여 분석 + 문서 파싱 구조 점검). 이후 모든 보정 변경은 이 하네스로 전후 실측이 원칙. 2.2.0 베이스라인은 `eval/baseline_2.2.0.txt`에 보존
- **JSON `oov_nouns` 필드 추가(최상위·문장별)**: 코퍼스에 없어 s5가 침묵하는 명사(2022 이후 신조어 등)를 노출. "검사 통과"가 아니라 "미검수"임을 소비자에게 전달하고, 웹 검색 등 외부 검증 대상 목록으로 사용 (기존에 프로젝트 단에서 하던 freq=0 웹검색 보완을 미검수 명사까지 확장하는 스킬 공식 기능화)
- **JSON `matched_groups` 필드 추가**: 매칭된 정규식의 현상 그룹명 목록
- **DB `build_info` 테이블 추가**: 코퍼스 출처, kiwipiepy 버전, 빌드 날짜 기록 (버전 불일치 진단용)

### Changed

- **s5 페어 추출기 정밀화** (`_extract_noun_verb_pairs`): ① VA+게(부사화 형용사) 건너뛰기 — "사진을 예쁘게 찍다"가 (사진,예쁘) 대신 (사진,찍)으로 추출, ② "N과 같이/같은"·"N보다" 비교 관용구의 N 제외 — "다음과 같이 묶입니다"가 (다음,묶이) 대신 (기업,묶이)로 추출, ③ 지시 용언(그렇/이렇/어떻) 페어 제외
- **동봉 DB를 원문 문장 파이프라인으로 재빌드**: co_n(인접 어절 공기표) 소비를 버리고 sentences.txt 100만 문장을 **런타임 추출기를 그대로 임포트해** 집계. DB-런타임 분포가 정의상 일치(2.1.3에서 확인된 불일치 버그 부류의 구조적 차단). 유의도는 Dunning LLR 자체 계산. 페어 57,579 → 163,272개, DB 4.3 → 12.5MB. (빵,사)·(사진,찍)·(인사,건네) 등 인접성 제약으로 0건이던 자연 결합 해소
- **정규식 사전을 현상 그룹 구조로 재편** (`AWKWARD_PATTERNS` → `AWKWARD_PATTERN_GROUPS`): 그룹당 1회 가산으로 중복 과금 제거, 확실(18)/강(12)/약(6) 3단계 가중. 확실 신호(이중수동, "~에 있어서" 등)는 단독으로 임계 통과, 약한 신호("의 경우", "것이다")는 보조 근거로만 작동. 융합 음절 클래스 보완(`[지진질집짐졌져]`) 및 신규 패턴 추가(하여금, 함으로써, ~을 수행/영위/획득/경험, 존재/가능 번역투 등). 하위 호환용 평면 `AWKWARD_PATTERNS`는 유지
- **s5 약한-신호 가지를 빈도 게이트로 재보정**: 자체 LLR 전환으로 in-DB 페어 55.6%가 sig<10이 되어 (생각,있) 같은 자연 결합이 대량 벌점받는 회귀를 실측으로 확인(2.1.1 회귀와 동일 부류). freq≤4 그리고 LLR<3.84(p<0.05 경계)일 때만 약한 벌점을 주도록 변경. 평가셋에서 약벌점 완전 제거와 동일 성능이면서 희소 의심 결합 검출력은 유지
- **s2 동사 카운트에 XSV/XSA 포함**: "정리하다/발견하다" 같은 하다동사가 동사로 안 세어져 평범한 문장의 명사/동사 비율이 치솟던 오판 수정
- **본문 추출 수정** (`extract_body_sentences`): ① 첫 본문 줄을 무조건 버리던 `skip_first_title` 제거 — 모든 문서의 첫 문단이 미평가되던 버그, ② 문장 종결형(마침표·다/요/죠)으로 끝나는 불릿을 평가에 포함 — 블로그 초안의 불릿 문장 사각지대 해소(명사 나열·항목 표기 불릿은 계속 제외)
- **대안 동사 추천에 기능동사 필터**: 대하/위하/통하/있 등 18종 스톱리스트 적용. "기업" 대안이 (대하, 많, 위하) → (거듭나, 늘, 가)로 개선
- **SKILL.md·README 정확도 서술 교체**: 검증 불가한 "거짓양성 0%" 주장을 삭제하고 평가셋 실측 수치와 해석 지침(의심 보고·OK 판정 각각의 한계)으로 대체

### 호환성

- 기존 JSON 키 모두 유지(`suspects`, `breakdown`, `matched`, `awkward_collocations`, `corpus_signal_active`, `signal_meaning` 등), `oov_nouns`·`matched_groups`만 신규
- 의심 임계값(17점), 워크플로우 4단계, `kiwipiepy` 의존성(0.23.1 확인) 동일
- `AWKWARD_PATTERNS` 평면 리스트는 하위 호환으로 유지 (그룹에서 자동 생성)
- 추출기 규칙과 DB가 함께 바뀌었으므로 **둘 중 하나만 구버전으로 롤백하면 안 됨** (추출-DB 정합 원칙)

## [2.2.0] - 2026-06-19

JSON/텍스트 출력에 `signal_meaning` 해석 규칙 필드를 추가하고, 시그널 5 0건의 해석 지침을 강화한 마이너 업데이트입니다. 검수 로직·시그널 가중치·임계값(17)은 변경되지 않았습니다.

### 배경

corpus_collocation freq=0을 LLM이 "거짓 양성"이라 임의 판단해 일축하는 사례가 반복됐습니다. freq=0은 코퍼스 기준으로는 그냥 '양성(적발)'이고, "거짓 양성"은 검사 결과가 아니라 모델이 사후에 붙이는 미검증 라벨입니다. 기존 SKILL.md는 오히려 "LLM이 문맥상 자연스럽다고 판단되면 생략"을 허용해, 적발을 의미 검증 없이 버리는 경로를 열어두고 있었습니다. 또 이 판정 규칙이 SKILL.md에만 적혀 있어, 스크립트만 실행하는 소비자(파이프라인 에이전트 등)에게는 닿지 않는 구조적 누수가 있었습니다.

### Added

- **`signal_meaning` 최상위 출력 필드 추가**: 모든 JSON/텍스트 출력에 0건 적발의 해석 규칙을 동봉. 명사 의미를 직접(지식·웹검색) 확인해 동사 적합성을 판정하기 전에는 일축 금지, 유지·수정 모두 근거 기록을 명시. 검사기 결과를 읽는 모든 소비자에게 규칙이 데이터와 함께 전달되도록 함

### Changed

- **SKILL.md "시그널 5 0건의 해석" 섹션 강화**: "LLM이 자연스럽다 판단되면 생략" 허용 문구를 제거하고, 의미 기반 동사 검증을 거치기 전에는 freq0 적발을 임의로 거짓 양성 처리할 수 없도록 명시. `signal_meaning` 출력 필드와 정합
- **버전 상향(2.1.3 → 2.2.0)**: JSON 출력 항목 확장(`signal_meaning`)이므로 PATCH 아닌 MINOR. 기존 키 호환 유지로 MAJOR 아님

### 호환성

- 기존 JSON 키(`suspects`, `breakdown`, `awkward_collocations`, `corpus_signal_active` 등) 모두 유지, `signal_meaning`만 신규 추가
- `assets/kor_collocation.db` 재빌드 불필요, `kiwipiepy` 의존성 동일, 검수 로직·임계값 변경 없음

## [2.1.3] - 2026-06-09

kiwipiepy 0.23+의 용언 하위태그(VV-R/VV-I/VA-R/VA-I) 누락 버그를 수정하고 동봉 DB를 재빌드한 패치입니다. JSON 스키마·워크플로우·임계값은 변경되지 않았습니다.

### 배경

kiwipiepy가 버전업되며 용언에 규칙/불규칙 하위태그를 붙이기 시작했는데, `scripts/check_naturalness.py`의 `_extract_noun_verb_pairs`와 `scripts/build_collocation_data.py`의 `normalize_word`가 둘 다 `tag in ('VV','VA')` 정확매칭을 써서 '잡/받/듣/새롭' 같은 동사를 통째로 누락했습니다. 추출기와 빌드가 같은 버그를 공유해 자기일관적이었던 탓에(양쪽이 같은 동사를 버려 거짓양성은 안 났음) 드러나지 않았고, s5가 해당 동사 부류를 평가하지 못하는 사각지대(거짓음성)만 존재했습니다.

### Fixed

- **`_extract_noun_verb_pairs`·`normalize_word` 태그매칭을 베이스 태그 비교(`tag.split('-')[0] in (...)`)로 수정**: 명사·동사 모두 적용. 추출기와 빌드가 동일 비교를 쓰도록 일치시켜 DB-런타임 불일치(자연 결합이 0건 거짓양성으로 잡히던 문제)를 차단

### Changed

- **`assets/kor_collocation.db` 재빌드**: 고유 동사 1,462 → 1,622종, 결합 페어 52,801 → 57,579개, 크기 약 3.9 → 4.3MB. (사진,받)·(심리,얼어붙) 등 그간 누락됐던 자연 결합이 이제 코퍼스에 적중해 관련 거짓양성 해소

### 검증 / 한계

- 실제 한국어 블로그 204문장 양성률: 60.8%(수정 전) → 62.3%(수정 후)로 **정확도는 개선되지 않음**. 태그 버그가 우연히 억제하던 거짓양성이, 수정으로 해당 동사들이 s5의 "코퍼스 0건 = +12" 로직에 노출되며 새로 발생해 상쇄됨(예: (패턴,새롭)·(자국,새기)는 자연스럽지만 뉴스 코퍼스 미수록이라 여전히 0건)
- 본 수정의 목적은 정확도 향상이 아니라 **추출–DB 일관성·kiwi 버전 견고성 확보**. 0건의 본질적 모호성(자연스럽지만 코퍼스 미수록 vs 진짜 어색)의 최종 판정은 설계상 LLM의 문맥 판단에 위임함(SKILL.md "시그널 5의 거짓 양성 처리")

### 호환성

- JSON 출력 스키마, 시그널 가중치, 의심 임계값(17점), 워크플로우 모두 동일
- `kiwipiepy` 의존성 동일 (0.23.1에서 확인)

## [2.1.2] - 2026-06-09

2.1.1에서 시도한 `COLLOCATION_SIG_THRESHOLD` 10.0 → 30.0 재보정이 거짓 양성을 늘리는 회귀로 확인돼 10.0으로 환원한 패치입니다. 2.1.1의 죽은 상수 제거는 옳았으므로 유지합니다.

### 배경 (2.1.1 재보정이 틀린 이유)

2.1.1은 "sig(로그우도)가 코퍼스 크기 N에 비례하니 임계값도 3배 올려 선택도를 유지한다"는 근거로 임계값을 30.0으로 올렸습니다. 이 추론은 임계값의 의미를 백분위로 착각한 오류였습니다. sig는 "이 공기빈도가 우연이 아닐 확신도"이고, **sig가 높을수록 잘 입증된 자연 결합, 낮을수록 우연과 구분 안 되는 의심 결합**입니다. 약한-신호 임계값은 "우연과 구분 안 되는 바닥"을 잡는 절대 기준이므로 N이 커져도 함께 올리면 안 됩니다. 올리면 잘 입증된 자연 결합까지 의심으로 쓸어담습니다.

### 검증 (회귀 실측)

- 임계값 30에서 약한-신호 벌점 대상이 in-DB 페어의 4.7% → **29.2%로 급증**. 새로 벌점받는 sig [10,30) 밴드 **12,918개(24.5%)**는 (중국,위하) freq97, (미국,통하) freq91, (정책,있) freq75, (결과,같) freq47 등 명백한 자연 결합 다수 포함
- 자연 문장 end-to-end: 밴드 페어를 포함한 자연 문장 6개 중 5개가 거짓 s5 벌점을 받고, "그는 미국을 통해 새 기술을 들여왔다"는 12점 → 18점으로 의심 문턱(17)을 넘겨 **거짓 양성으로 전환**
- 2.1.0이 거짓 양성 해소라고 기록한 (사례,모으)는 sig 30.8로 임계값 30을 간신히 통과 — 임계값이 조금만 더 높았어도 그 수정이 회귀할 뻔함

### Reverted

- **`scripts/check_naturalness.py` `COLLOCATION_SIG_THRESHOLD` 30.0 → 10.0**: 2.0.0/2.1.0에서 검증된 원래 값으로 환원. 코드 주석에 "sig 임계값은 절대 기준이라 코퍼스 크기와 함께 올리면 안 된다"는 이유를 명시해 재발 방지

### Unchanged (2.1.1에서 유지)

- 죽은 상수 `COLLOCATION_FREQ_THRESHOLD` 제거는 동작과 무관한 옳은 정리라 그대로 유지

### 호환성

- JSON 출력 스키마, 시그널 가중치, 의심 임계값(17점), 워크플로우 모두 동일
- `assets/kor_collocation.db` 재빌드 불필요

## [2.1.1] - 2026-06-08

2.1.0의 1M 코퍼스 업그레이드에 맞춰 시그널 5(코퍼스 결합 빈도 룩업)의 유의도 임계값을 재보정하고 미사용 상수를 제거한 패치입니다. JSON 스키마·워크플로우·시그널 가중치는 변경되지 않았습니다.

### 배경

`sig`(Leipzig 로그우도)는 코퍼스 크기 N에 비례해 커집니다. 2.1.0에서 300K → 1M(3.3배)로 확장하면서 sig 값 분포 전체가 약 3배 부풀려졌으나 `COLLOCATION_SIG_THRESHOLD`는 10.0으로 유지돼, 신 DB에서는 in-DB 페어의 4.7%만 임계 미만으로 걸렸습니다(평균 sig 100.9). 그 결과 "약한 신호(6점)" 가지가 사실상 무력화돼 시그널 5가 0건 여부만 보는 이진 판정으로 퇴화한 상태였습니다.

### Changed

- **`scripts/check_naturalness.py` `COLLOCATION_SIG_THRESHOLD` 10.0 → 30.0**: 코퍼스 3.3배 확장에 맞춰 약 3배 상향해 약한-신호 가지의 선택도를 2.0.0 시절 수준으로 복원. 코퍼스 크기와 sig 임계값을 함께 조정해야 하는 이유를 코드 주석으로 명시

### Removed

- **`scripts/check_naturalness.py` `COLLOCATION_FREQ_THRESHOLD` 상수 삭제**: 정의만 있고 코드 어디서도 참조되지 않던 죽은 상수. "0건 = 강한 신호" 판정은 이 상수가 아니라 DB 룩업 결과가 `None`인지로 처리되므로 제거해도 동작 변화 없음

### 호환성

- JSON 출력 스키마, 시그널 가중치, 의심 임계값(17점), 워크플로우 모두 2.1.0과 동일
- `assets/kor_collocation.db` 재빌드 불필요 (룩업 코드만 변경)
- `kiwipiepy` 의존성 동일

## [2.1.0] - 2026-06-06

동봉 코퍼스 DB를 Leipzig kor_news_2022_300K에서 kor_news_2022_1M(3.3배 규모)로 업그레이드한 마이너 업데이트입니다. 시그널 5(코퍼스 결합 빈도 룩업)의 거짓 양성을 줄이는 데이터 확장이며, 워크플로우·JSON 스키마·임계값은 변경되지 않았습니다.

### 배경

2.0.0의 알려진 한계는 300K 코퍼스의 희소성이었습니다. 자연스러운 결합도 코퍼스에 0건으로 나와 거짓 양성이 발생했습니다(예: 사례+모으). 대안으로 Hugging Face의 HAERAE-HUB/KOREAN-WEBTEXT(22억 토큰)와 KORMo-Team/korean-public-corpus(27.5GB)를 검토했으나 채택하지 않았습니다.

- **KOREAN-WEBTEXT 기각**: 웹 크롤 텍스트(CC100/OSCAR 2018~2023)에는 기계번역체가 섞여 있어, 이 스킬이 잡으려는 번역투가 "자연" 참조 분포에 오염될 위험이 목적과 정면 충돌
- **korean-public-corpus 기각**: WildBoar-LM/Korean-opensource의 미러로 README·라이선스·수집 시기 문서화 전무, 학술논문 편중(보고서투 결합이 자연으로 등록될 위험), PDF 추출 띄어쓰기 노이즈
- **두 후보 모두 텍스트 작성 시기가 2022 뉴스보다 새롭지 않음** (공개일만 2024/2025)
- 라이프치히 다운로드 서버 직접 조회 결과 kor_news 2023년 이후판 미제공(2026-06 확인) — 1M이 동일 파이프라인으로 가능한 최대 확장

1M 업그레이드는 사전 계산된 공기 빈도표(co_n.txt)를 그대로 소비해 빌드 스크립트 URL 한 줄 교체로 끝나며, 편집된 뉴스 한국어라는 참조 분포의 순도를 유지합니다.

### Changed

- **`assets/kor_collocation.db` 재빌드 (300K → 1M)**: 결합 페어 16,051 → 52,801개(3.3배), 명사 4,572 → 10,210개, 동사 818 → 1,462개. DB 크기 약 1.2MB → 약 3.9MB
- **`scripts/build_collocation_data.py`**: `URL` 상수를 kor_news_2022_1M로 변경, 다운로드 캐시 파일명을 URL에서 자동 유도하도록 수정
- **SKILL.md 코퍼스 참조 갱신**: 출처·크기 표기 1M 기준으로 변경, 거짓 양성 예시를 1M에서도 유효한 페어(음성, 담)로 교체, 코퍼스 갱신 절차에 Leipzig 한계와 향후 확장 방향(원시 텍스트 파이프라인 재작성 필요, 웹 크롤 텍스트 기피 원칙) 명시
- **MINOR 버전 상향(2.0.0 → 2.1.0)**: 시그널 5의 데이터 기반 전면 교체로 검출 커버리지가 확장됨. 스키마·워크플로우 호환은 유지되므로 MAJOR 아님, 단순 패턴 추가·미세 조정을 넘는 변경이므로 PATCH 아님

### 검증

- 구 DB 대비 회귀 없음: 구 DB에서 0건이던 어색 페어(기업+묶 등)는 신 DB에서도 0건 유지
- 자연 페어 강화: (기업, 대하) freq 77→290, (인사, 건네) 27→94, (사례, 모으) 0→4 신규 등록(2.0.0 거짓 양성 해소)
- 엔드투엔드: "분야별 관심 기업이 다음과 같이 묶입니다" → 시그널 5 가산 12점 포함 18.7점으로 의심 검출, 대안 동사 5개 제시. "인사를 건넸습니다" → 0.0점 통과. 어휘 사전 시그널(가지고 있다는 점에서) 정상 동작
- `corpus_signal_active: true` 확인

### 호환성

- JSON 출력 스키마, 시그널 가중치, 임계값(17점) 모두 2.0.0과 동일
- `scripts/check_naturalness.py` 변경 없음
- `kiwipiepy` 의존성 동일

## [2.0.0] - 2026-05-11

한국어 자연 코퍼스(Leipzig kor_news_2022_300K) 기반 명사+동사 결합 빈도 룩업을 5번째 시그널로 추가한 메이저 업데이트입니다. 기존 형태소 통계 + 정규식 사전 결합 방식의 한계였던 "사전에 등록되지 않은 어색 결합" 문제를 코퍼스 룩업으로 보완합니다.

### 배경

1.0.x 버전은 정규식 사전(`AWKWARD_PATTERNS`)에 등록된 패턴만 검출할 수 있었습니다. "기업이 묶입니다", "사례를 묶음으로" 같이 한 번 등장해도 한국어에서 매우 어색한 결합이 사전에 등록되지 않으면 그대로 통과했습니다. 사용자 본문 수정 사례 누적 로그가 쌓일수록 사전이 확장되긴 하지만, LLM이 새로 만들어낼 수 있는 어색 결합의 변이는 무한해 사전 관리만으로는 한계가 명확했습니다.

해법은 한국어 자연 코퍼스에서 (명사, 동사) 결합 빈도를 직접 룩업하는 것입니다. 코퍼스에 0건이거나 통계적 유의도가 낮은 결합은 "한국인은 이 자리에 이 동사를 거의 안 쓴다"는 외부 통계 신호로 활용할 수 있습니다. namuwikitext(5GB) 같은 대형 코퍼스 다운로드 방식은 Claude.ai 웹 환경에서 영구 저장 불가 문제로 적합하지 않았습니다. 대신 Leipzig 패키지에 사전 계산되어 동봉되는 좌/우 인접 공기 빈도 표(co_n.txt)를 형태소 분석으로 명사+동사 결합만 추출해 SQLite DB(약 1.2MB)로 빌드하면, 스킬 패키지에 동봉할 수 있어 양쪽 환경 모두에서 즉시 동작합니다.

### Added

- **`scripts/build_collocation_data.py` 신규 추가**: 스킬 개발자용 빌드 스크립트. Leipzig kor_news_2022_300K 패키지(CC BY 4.0)를 다운로드해 형태소 분석으로 명사+동사 어간 결합만 추출한 뒤 SQLite DB로 빌드. 사용자가 실행할 필요 없음. 향후 코퍼스 갱신·확장 시 재빌드용
- **`assets/kor_collocation.db` 동봉 데이터 추가**: 빌드 결과 SQLite DB. 16,051개 (명사, 동사) 결합 페어, 명사 4,572개, 동사 818개. 약 1.2MB로 스킬 패키지에 동봉됨. Claude.ai 웹과 PC Claude Code 양쪽에서 별도 셋업 없이 즉시 활용
- **`scripts/check_naturalness.py`에 시그널 5 추가**: 코퍼스 결합 빈도 룩업. 본문에서 (명사, 동사 어간) 페어를 추출해 동봉 DB에서 검색. 결합 0건이면 12점 가산, 유의도(로그우도) 10 미만이면 6점 가산, 시그널 5 최대 20점. 어색 페어 발견 시 같은 명사로 자주 결합하는 자연 동사 상위 5개를 대안으로 함께 출력
- **JSON 출력 스키마 확장**: `breakdown.corpus_collocation` 필드 추가, `awkward_collocations` 배열 필드 추가(각 어색 페어와 자연 대안 동사 포함), `corpus_signal_active` 최상위 필드 추가(동봉 DB 존재 여부)
- **SKILL.md "코퍼스 룩업 결과 가공" 섹션 추가**: 시그널 5의 거짓 양성 처리 가이드, 사용자 보고 형식, 코퍼스 대안 동사 활용 원칙 명시
- **SKILL.md "동봉 코퍼스 데이터 관리" 섹션 추가**: 동봉 DB의 출처, 라이선스, 갱신 절차 명시

### Changed

- **자체 검수 편향 보완 범위 확대**: 기존에는 사전에 등록된 패턴만 잡았지만, 시그널 5 추가로 사전에 없는 새로운 어색 결합도 자동 검출. 사용자 본문 수정 사례 누적 로그에 의존하던 사전 확장 부담이 줄어듦
- **MAJOR 버전 상향(1.0.1 → 2.0.0)**: JSON 출력 스키마에 신규 필드 추가(`corpus_collocation`, `awkward_collocations`, `corpus_signal_active`)는 기존 키 호환은 유지되지만 새 필드 활용 코드는 신버전에 의존하므로 MAJOR 상향

### 호환성

- 1.x 버전과 동일한 JSON 키(`noun_density`, `noun_verb_ratio`, `bound_josa_chains`, `awkward_lexical`)는 그대로 유지
- 동봉 DB가 누락된 환경에서는 `corpus_signal_active: false`로 동작하며 1~4 시그널은 정상. 단 점수 분포는 5 시그널 비활성으로 약간 낮아짐
- `kiwipiepy` 의존성은 동일

### 검증

- "기업+묶" 0건 → 정확하게 어색 검출
- "끈+묶", "발+묶이", "채+묶이" 자연 결합은 코퍼스에 다수 매칭 → 정확하게 자연 판정
- 본문 시험에서 시그널 5가 새로 잡은 어색 결합 다수 검출됨(예: "음성, 담", "인사, 건네" 등 자연 페어 일부도 0건 잡힘 - 거짓 양성. 임계 점수와 LLM 문맥 판단으로 거짓 양성은 사용자 보고에서 자동 제외 가능)

### Dependencies

- `kiwipiepy`: 변동 없음
- 신규 의존성 없음 (sqlite3는 Python 표준 라이브러리)

## [1.0.1] - 2026-05-10

Anthropic 프롬프트 가이드(claude-prompting-best-practices) 준수 여부를 검토하고 SKILL.md 본문을 보완한 패치 버전입니다. 워크플로우와 산출물은 변경되지 않았습니다.

### 배경

1.0.0 출시 후 SKILL.md를 Anthropic 공식 프롬프트 가이드 기준으로 재검토한 결과, 몇 가지 권장 패턴이 누락된 것이 확인됐습니다. 가이드는 시스템 프롬프트 성격의 문서(스킬 본문 포함)에서 다음을 권장합니다.

- XML 태그로 컨텍스트, 워크플로우, 예시 영역을 명확히 구분 (`<workflow>`, `<example>`, `<input_handling>` 등)
- 부정 표현(~하지 말고)보다 긍정 표현(~합니다)이 LLM 지시 따르기에 더 효과적
- 추상 지시보다 구체 입출력 예시(few-shot) 3~5개가 출력 일관성을 크게 끌어올림
- 모든 지시에 이유(why)를 함께 적으면 LLM이 새로운 상황에 일반화함
- Opus 4.7의 literal instruction following 특성상 scope를 명시해야 의도대로 작동

이번 패치는 위 5가지를 본문에 적용했습니다. 산출물 형식과 검수 로직은 1.0.0과 동일합니다.

### Changed

- **SKILL.md XML 태그 도입**: `<workflow>`, `<input_handling>`, `<output_format>`, `<example>` 태그로 영역 구분. 마크다운 헤더만으로 구분하던 1.0.0 대비 LLM이 영역 경계를 더 명확히 인식
- **SKILL.md 부정 표현 → 긍정 표현 재작성**: "JSON을 그대로 보여주지 말고", "정규식 그대로 보여주지 말고" 같은 부정문을 "한국어로 풀어 설명합니다", "정규식을 한국어 설명으로 옮깁니다" 같은 긍정문으로 변경
- **SKILL.md full input/output 예시 3개 추가**: (1) 어색 본문 2문장 → JSON → 사용자 보고 출력까지 풀 예시, (2) 의심 0건 OK 케이스 풀 예시, (3) 어색 패턴 사전 누적 좋은/나쁜 예시. 1.0.0은 형식 템플릿만 있고 구체 실행 예시는 없었음
- **SKILL.md 이유 설명 보강**: "JSON 모드 사용", "추측해서 임의 파일 검수 안 함", "평가와 수정 분리", "대안 1~2개만 제시" 등 모든 핵심 지시에 그 이유를 한 문장으로 추가
- **SKILL.md scope 명시 보강**: "본문 자동 수정은 글 작성 스킬이나 사용자 후속 지시에서 처리" 같은 책임 범위를 명시적으로 적어 Opus 4.7의 literal instruction following과 정합

### Unchanged

- `scripts/check_naturalness.py`는 변경 없음. 검수 로직, 시그널 가중치, 임계값 모두 1.0.0과 동일
- 워크플로우 4단계 동일
- JSON 출력 스키마 동일

## [1.0.0] - 2026-05-10

한국어 자연스러움 평가를 독립 스킬로 분리한 초기 릴리즈입니다. 기존 `naver-blog-studio-v2` 2.3.0에 통합돼 있던 검수 로직을 단독 스킬로 추출하고, 다른 글 작성 스킬(`naver-blog-review-writer`, `naver-shopping-connect` 등)에서도 동일하게 호출 가능한 범용 평가 도구로 만들었습니다.

### 배경

이전까지 검수 로직은 `naver-blog-studio-v2` 안에서만 동작했습니다. 다른 글 작성 스킬이 늘어나면서 같은 검수 로직을 각 스킬마다 통합하면 어휘 사전이 분산되고 동기화 부담이 커지는 문제가 확인됐습니다. 검수는 글 작성 워크플로우와 직교적이므로 별도 스킬로 분리하는 것이 자연스러웠습니다.

KoGPT-2 같은 외부 한국어 모델을 활용하는 방안도 검토됐으나, Claude.ai 웹 환경에서 외부 모델 다운로드와 추론이 차단돼 PC와 웹 동기화 조건과 충돌했습니다. 형태소 분석기 `kiwipiepy`와 자체 구축한 어색 어휘 정규식 사전을 결합하는 방식이 양쪽 환경에서 동일하게 동작하면서도 LLM 자체 검수와 다른 분포의 외부 시그널을 제공할 수 있어 채택했습니다.

### Added

- **`scripts/check_naturalness.py` 신규 추가**: 형태소 통계와 어색 어휘 사전 결합 방식 한국어 검수 스크립트. 4개 시그널(명사 밀도, 명사 동사 비율, 의존명사 조사 연쇄, 어색 어휘 매칭)을 합산해 0~100 점수를 산출하고, 임계 17점 이상 문장을 의심 문장으로 보고. JSON 출력 모드(`--json`) 제공으로 LLM 후처리에 적합
- **`SKILL.md` 워크플로우 정의**: 입력 정규화(파일 경로 또는 인라인 텍스트), 의존성 확인, 스크립트 실행, 사용자 보고의 4단계 단일 워크플로우 정의
- **권장 수정 방향 제시 규칙**: 각 의심 문장마다 LLM이 한국어 감각으로 자연스러운 대안 1~2개를 제시. 정규식 패턴은 사용자에게 한국어로 풀어 설명. 자동 수정은 하지 않음

### 검증 정확도

- 같은 유형 페어 100% (자연 평균 1.3점, 어색 평균 34.2점)
- OOD 페어 71% (자연 8/8, 어색 4/6)
- 자연 문장 거짓 양성 0%

### Dependencies

- `kiwipiepy`. 최초 1회 `pip install --break-system-packages kiwipiepy` 실행 필요. Claude.ai 웹 컨테이너와 PC 환경 양쪽에서 설치 가능
